generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["partialIndexes", "views"]
}

generator prismabox {
  provider                    = "prismabox"
  previewFeatures             = ["partialIndexes"]
  inputModel                  = "true"
  typeboxImportDependencyName = "elysia"
  typeboxImportVariableName   = "t"
}

datasource db {
  provider = "postgresql"
  schemas  = ["3nconnect", "auth", "public"]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_attribute_groups {
  id                     Int                   @id @default(autoincrement())
  attr_group_name        String?
  attr_group_description String?
  is_active              Boolean?
  created_at             DateTime              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?             @db.Timestamptz(6)
  attributes             nconnect_attributes[]

  @@map("attribute_groups")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_attributes {
  id               Int                        @id @default(autoincrement())
  attr_group_id    Int?
  attr_name        String?
  attr_description String?
  is_active        Boolean?
  created_at       DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?                  @db.Timestamptz(6)
  attribute_groups nconnect_attribute_groups? @relation(fields: [attr_group_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("attributes")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_brands {
  id          Int                 @id @default(autoincrement())
  brand_image String?
  url_image   String?
  brand_name  String
  country_id  Int?
  is_active   Boolean
  created_at  DateTime            @default(now()) @db.Timestamptz(6)
  updated_at  DateTime?           @db.Timestamptz(6)
  products    nconnect_products[]

  @@map("brands")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cart_reviews {
  id                           Int       @id @default(autoincrement())
  review_token                 String?
  expires_at                   DateTime? @db.Timestamptz(6)
  requires_sign_in_for_voucher Boolean?  @default(true)
  requires_sign_in_for_payment Boolean?  @default(true)
  created_at                   DateTime  @default(now()) @db.Timestamptz(6)
  customeruser_id              String?   @default(dbgenerated("gen_random_uuid()")) @db.Uuid

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_companies {
  id            Int                 @id @default(autoincrement())
  company_image String?
  url_image     String?
  company_name  String?
  created_at    DateTime            @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?           @db.Timestamptz(6)
  is_active     Boolean
  order_billing order_billing[]
  products      nconnect_products[]

  @@map("companies")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_countries {
  id           Int       @id @default(autoincrement())
  country_name String?
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  updated_at   DateTime? @db.Timestamptz(6)
  is_active    Boolean?
  short_name   String?

  @@map("countries")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_address {
  id              Int             @id @default(autoincrement())
  customeruser_id String?         @db.Uuid
  tag_name        String?
  receiver_name   String?
  phone_no        String?
  address_name    String?
  address_line1   String?
  address_line2   String?
  sub_district    String?
  district        String?
  province        String?
  post_code       String?
  set_default     Boolean?
  order_billing   order_billing[]

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_redeem_code {
  id              Int       @id @default(autoincrement())
  customeruser_id String?   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  gift_voucher_id Int?
  used_at         DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model event_categories {
  id              Int               @id @default(autoincrement())
  event_cate_name String?
  created_at      DateTime          @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?         @db.Timestamptz(6)
  is_active       Boolean?
  events          nconnect_events[]

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_events {
  id                Int               @id @default(autoincrement())
  event_name        String?
  event_detail      String?
  register_date     DateTime?         @db.Date
  preregister_date  DateTime?         @db.Date
  event_startdate   DateTime?         @db.Date
  event_enddate     DateTime?         @db.Date
  event_image       String?
  location_name     String?
  event_category_id Int?
  tier_register     String[]
  tier_preregister  String[]
  ref_url           String?
  created_at        DateTime          @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?         @db.Timestamptz(6)
  is_active         Boolean?
  event_categories  event_categories? @relation(fields: [event_category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("events")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model favorite_products {
  id                Int       @id @default(autoincrement())
  product_option_id Int?
  customeruser_id   String?   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at        DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model gift_voucher {
  id                            Int                             @id @default(autoincrement())
  url_image                     String?
  voucher_image                 String?
  voucher_name                  String?
  voucher_description           String?
  voucher_conditions            String?
  campaign_start                DateTime?                       @db.Timestamptz(6)
  campaign_end                  DateTime?                       @db.Timestamptz(6)
  customer_tiers                String[]
  specific_customers            String[]
  is_accept_overlapse_promotion Boolean?
  is_limit_voucher              Boolean?
  is_specific_customers         Boolean?
  is_active                     Boolean?
  created_at                    DateTime                        @default(now()) @db.Timestamptz(6)
  updated_at                    DateTime?                       @db.Timestamptz(6)
  gift_voucher_type             String?                         @default("")
  gift_voucher_method           String?
  voucher_uuid                  String                          @db.Uuid
  limited_total_quantity        Int?
  is_lifetime_period            Boolean?
  usage_period_day              Int?
  event_trigger                 String?
  tier_trigger_name             String?
  gift_voucher_campaign_voucher gift_voucher_campaign_voucher[]
  gift_voucher_generic          gift_voucher_generic[]
  gift_voucher_redeem_code      gift_voucher_redeem_code[]

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model gift_voucher_campaign_voucher {
  id                 Int           @id(map: "gift_voucher_event_pkey") @default(autoincrement())
  gift_voucher_id    Int?
  generic_voucher_id Int?
  gift_voucher       gift_voucher? @relation(fields: [gift_voucher_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model gift_voucher_generic {
  id               Int           @id @default(autoincrement())
  gift_voucher_id  Int?
  discount_type    String?
  min_purchase     Float?
  percent_discount Float?
  max_discount     Float?
  usage_period_day Int?
  gift_voucher     gift_voucher? @relation(fields: [gift_voucher_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model gift_voucher_redeem_code {
  id                     Int           @id @default(autoincrement())
  gift_voucher_id        Int?
  redeem_code            String?
  max_discount           Float?
  max_usage_per_customer Int?
  gift_voucher           gift_voucher? @relation(fields: [gift_voucher_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_billing {
  id                     Int                   @id(map: "customer_order_billing_pkey") @default(autoincrement())
  order_no               String?
  buyer_customeruser_id  String                @db.Uuid
  payment_method_type    String?
  order_status           String?
  im_no                  String?
  order_type             String?
  invoice_id             Int?
  shipping_address_id    Int?
  payment_status         String?
  log_payment            DateTime?             @db.Timestamptz(6)
  order_uuid             String?               @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at             DateTime              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?             @db.Timestamptz(6)
  admin_updated_by       String?               @db.Uuid
  admin_updated_at       DateTime?             @db.Timestamptz(6)
  order_created_by       String?               @db.Uuid
  contact_id             Int?
  company_id             Int?
  shipping_cost          Float?
  admin_verify_status    String?
  payment_invoice_no     String?
  credit_terms_day       Int?
  is_admin_order_created Boolean?
  companies              nconnect_companies?   @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_address       customer_address?     @relation(fields: [shipping_address_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  order_billing_items    order_billing_items[]

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_billing_items {
  id                               Int            @id @default(autoincrement())
  order_billing_id                 Int?
  product_option_id                Int?
  order_product_quantity           Int?
  item_status                      String?
  mr_code                          String?
  localtion_code                   String?
  product_owner                    String?
  expire_date                      DateTime?      @db.Date
  lot_code                         String?
  sale_price                       Float?
  order_price                      Float?
  waiting_out_quantity             Int?
  admin_updated_by                 String?        @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  admin_updated_at                 DateTime?      @db.Timestamptz(6)
  is_free                          Boolean?
  promotion_from_product_option_id Int?
  order_billing                    order_billing? @relation(fields: [order_billing_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_billing_payment {
  id                   Int     @id @default(autoincrement())
  invoice_no           String?
  merchant_id          String?
  description          String?
  amount               Float?
  currency_code        String? @default("THB")
  payment_channel_code String?
  payment_agent_code   String?
  response_url         String?
  backend_url          String?

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_billing_payment_response {
  id                   Int       @id @default(autoincrement())
  invoice_no           String?
  amount               Float?
  currency_code        String?   @default("THB")
  tran_ref             String?
  reference_no         String?
  payment_agent_code   String?
  payment_channel_code String?
  approval_code        String?
  datetime             DateTime? @db.Timestamptz(6)
  response_code        String?
  response_description String?
  card_info            Json?     @db.Json

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_billing_voucher_usage {
  id                Int       @id @default(autoincrement())
  order_billing_id  Int?
  gift_voucher_code String?
  promotion_id      Int?
  usaged_at         DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_product_categories {
  id                       String                        @id(map: "categories_pkey")
  name                     String
  slug                     String                        @unique(map: "categories_slug_unique")
  parent_id                String?
  ancestors                String[]                      @default([])
  level                    Int                           @default(0) @db.SmallInt
  sort_order               Int                           @default(0) @db.SmallInt
  image_url                String?
  is_active                Boolean                       @default(true)
  created_at               DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                     @db.Timestamptz(6)
  product_categories       nconnect_product_categories?  @relation("product_categoriesToproduct_categories", fields: [parent_id], references: [id], onUpdate: NoAction, map: "categories_parent_id_fkey")
  other_product_categories nconnect_product_categories[] @relation("product_categoriesToproduct_categories")
  products                 nconnect_products[]

  @@index([ancestors], map: "idx_categories_ancestors", type: Gin)
  @@index([is_active], map: "idx_categories_is_active")
  @@index([level], map: "idx_categories_level")
  @@index([parent_id], map: "idx_categories_parent_id")
  @@index([sort_order], map: "idx_categories_sort_order")
  @@map("product_categories")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_product_images {
  id         Int                @id @default(autoincrement())
  product_id Int?
  url_image  String?
  is_show    Boolean?
  created_at DateTime           @default(now()) @db.Timestamptz(6)
  updated_at DateTime?          @db.Timestamptz(6)
  products   nconnect_products? @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("product_images")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_product_options {
  id                   Int                @id @default(autoincrement())
  product_id           Int?
  mat_identity         String?
  option_name          String?
  online_price         Float?
  min_price            Float?
  created_at           DateTime           @default(now()) @db.Timestamptz(6)
  row_no               Int?
  updated_at           DateTime?          @db.Timestamptz(6)
  is_stock             Boolean?
  attributes_hierarchy Int[]
  products             nconnect_products? @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("product_options")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_product_payment_method {
  id                Int                @id @default(autoincrement())
  product_id        Int?
  qr_code_promptpay Boolean?
  visa_card         Boolean?
  mobile_banking    Boolean?
  credit_terms      Boolean?
  created_at        DateTime           @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?          @db.Timestamptz(6)
  products          nconnect_products? @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("product_payment_method")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_products {
  id                     Int                               @id @default(autoincrement())
  product_name           String?
  product_description    String?
  company_id             Int?
  brand_id               Int?
  created_at             DateTime                          @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                         @db.Timestamptz(6)
  is_active              Boolean?
  category_hierarchy     String[]
  unit                   String?
  is_online_active       Boolean?
  category_id            String?
  video_product          String?
  condition_description  String?
  warranty_description   String?
  youtube_url            String?
  is_pre_order           Boolean?
  is_custom_options      Boolean?
  product_images         nconnect_product_images[]
  product_options        nconnect_product_options[]
  product_payment_method nconnect_product_payment_method[]
  brands                 nconnect_brands?                  @relation(fields: [brand_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_categories     nconnect_product_categories?      @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  companies              nconnect_companies?               @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_bundle_deal_free_products {
  id                                 Int                                          @id @default(autoincrement())
  bundle_deal_get_id                 Int?
  product_option_id                  Int?
  free_quantity                      Int?
  promotion_bundle_deal_get_products nconnect_promotion_bundle_deal_get_products? @relation(fields: [bundle_deal_get_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_bundle_deal_free_products_bundle_get_id_fkey")

  @@map("promotion_bundle_deal_free_products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_bundle_deal_get_products {
  id                                  Int                                            @id(map: "promotion_bundle_deal_products_pkey") @default(autoincrement())
  promotion_id                        Int?
  product_option_id                   Int?
  get_quantity                        Int?
  promotion_bundle_deal_free_products nconnect_promotion_bundle_deal_free_products[]
  promotions                          promotions?                                    @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_bundle_deal_products_promotion_id_fkey")

  @@map("promotion_bundle_deal_get_products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_bundle_deal_grand_total_free_products {
  id                                      Int                                               @id(map: "promotion_bundle_deal_grand_total_free_pkey") @default(autoincrement())
  bundle_deal_grand_total_tiers_id        Int?
  product_option_id                       Int?
  free_quantity                           Int?
  promotion_bundle_deal_grand_total_tiers nconnect_promotion_bundle_deal_grand_total_tiers? @relation(fields: [bundle_deal_grand_total_tiers_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_bundle_deal_grand_t_bundle_deal_grand_total_tier_fkey")

  @@map("promotion_bundle_deal_grand_total_free_products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_bundle_deal_grand_total_tiers {
  id                                              Int                                                        @id @default(autoincrement())
  promotion_id                                    Int?
  level_no                                        Int?
  minimum_grand_total                             Float?
  promotion_bundle_deal_grand_total_free_products nconnect_promotion_bundle_deal_grand_total_free_products[]
  promotions                                      promotions?                                                @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("promotion_bundle_deal_grand_total_tiers")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_discount_products {
  id                Int         @id(map: "discount_products_pkey") @default(autoincrement())
  promotion_id      Int?
  product_option_id Int?
  sale_price        Float?
  sale_percent      Float?
  promotions        promotions? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("promotion_discount_products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_extra_points_products {
  id                Int         @id(map: "promotion_extra_points_product_point_pkey") @default(autoincrement())
  promotion_id      Int?
  product_option_id Int?
  points_multiplier Float?
  promotions        promotions? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_extra_points_product_point_promotion_id_fkey")

  @@map("promotion_extra_points_products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_extra_points_tier_rules {
  id                Int         @id @default(autoincrement())
  promotion_id      Int?
  level_no          Int?
  min_amount        Float?
  points_multiplier Float?
  promotions        promotions? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("promotion_extra_points_tier_rules")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model nconnect_promotion_flashsale_products {
  id                Int         @id(map: "flashsale_products_pkey") @default(autoincrement())
  promotion_id      Int?
  product_option_id Int?
  sale_price        Float?
  sale_percent      Float?
  promotions        promotions? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("promotion_flashsale_products")
  @@schema("3nconnect")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model promotions {
  id                                      Int                                                @id @default(autoincrement())
  url_image                               String?
  promotion_image                         String?
  promotion_name                          String?
  promotion_type                          String?
  promotion_start                         DateTime?                                          @db.Timestamptz(6)
  promotion_end                           DateTime?                                          @db.Timestamptz(6)
  created_at                              DateTime                                           @default(now()) @db.Timestamptz(6)
  updated_at                              DateTime?                                          @db.Timestamptz(6)
  is_active                               Boolean?
  is_accept_overlapse_promotion           Boolean?
  promotion_description                   String?
  customer_tiers                          String[]
  promotion_bundle_deal_get_products      nconnect_promotion_bundle_deal_get_products[]
  promotion_bundle_deal_grand_total_tiers nconnect_promotion_bundle_deal_grand_total_tiers[]
  promotion_discount_products             nconnect_promotion_discount_products[]
  promotion_extra_points_products         nconnect_promotion_extra_points_products[]
  promotion_extra_points_tier_rules       nconnect_promotion_extra_points_tier_rules[]
  promotion_flashsale_products            nconnect_promotion_flashsale_products[]

  @@schema("3nconnect")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model custom_oauth_providers {
  id                      String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider_type           String
  identifier              String    @unique
  name                    String
  client_id               String
  client_secret           String
  acceptable_client_ids   String[]  @default([])
  scopes                  String[]  @default([])
  pkce_enabled            Boolean   @default(true)
  attribute_mapping       Json      @default("{}")
  authorization_params    Json      @default("{}")
  enabled                 Boolean   @default(true)
  email_optional          Boolean   @default(false)
  issuer                  String?
  discovery_url           String?
  skip_nonce_check        Boolean   @default(false)
  cached_discovery        Json?
  discovery_cached_at     DateTime? @db.Timestamptz(6)
  authorization_url       String?
  token_url               String?
  userinfo_url            String?
  jwks_uri                String?
  created_at              DateTime  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime  @default(now()) @db.Timestamptz(6)
  custom_claims_allowlist String[]  @default([])

  @@index([created_at])
  @@index([enabled])
  @@index([identifier])
  @@index([provider_type])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                 @id @db.Uuid
  user_id                String?                @db.Uuid
  auth_code              String?
  code_challenge_method  code_challenge_method?
  code_challenge         String?
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?              @db.Timestamptz(6)
  updated_at             DateTime?              @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?              @db.Timestamptz(6)
  invite_token           String?
  referrer               String?
  oauth_client_state_id  String?                @db.Uuid
  linking_target_id      String?                @db.Uuid
  email_optional         Boolean                @default(false)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String     @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime?  @db.Timestamptz(6)
  created_at      DateTime?  @db.Timestamptz(6)
  updated_at      DateTime?  @db.Timestamptz(6)
  email           String?    @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        auth_users       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([friendly_name, user_id], map: "mfa_factors_user_friendly_name_unique", where: raw("(TRIM(BOTH FROM friendly_name) <> ''::text)"))
  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 auth_users?                @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at], map: "oauth_auth_pending_exp_idx", where: raw("(status = 'pending'::auth.oauth_authorization_status)"))
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                         String                  @id @db.Uuid
  client_secret_hash         String?
  registration_type          oauth_registration_type
  redirect_uris              String
  grant_types                String
  client_name                String?
  client_uri                 String?
  logo_uri                   String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?               @db.Timestamptz(6)
  client_type                oauth_client_type       @default(confidential)
  token_endpoint_auth_method String
  oauth_authorizations       oauth_authorizations[]
  oauth_consents             oauth_consents[]
  sessions                   sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         auth_users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([client_id], map: "oauth_consents_active_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, client_id], map: "oauth_consents_active_user_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      auth_users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  auth_users       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model auth_users {
  instance_id                                     String?                @db.Uuid
  id                                              String                 @id @db.Uuid
  aud                                             String?                @db.VarChar(255)
  role                                            String?                @db.VarChar(255)
  email                                           String?                @unique(map: "users_email_partial_key", where: raw("(is_sso_user = false)")) @db.VarChar(255)
  encrypted_password                              String?                @db.VarChar(255)
  email_confirmed_at                              DateTime?              @db.Timestamptz(6)
  invited_at                                      DateTime?              @db.Timestamptz(6)
  confirmation_token                              String?                @unique(map: "confirmation_token_idx", where: raw("((confirmation_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  confirmation_sent_at                            DateTime?              @db.Timestamptz(6)
  recovery_token                                  String?                @unique(map: "recovery_token_idx", where: raw("((recovery_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  recovery_sent_at                                DateTime?              @db.Timestamptz(6)
  email_change_token_new                          String?                @unique(map: "email_change_token_new_idx", where: raw("((email_change_token_new)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  email_change                                    String?                @db.VarChar(255)
  email_change_sent_at                            DateTime?              @db.Timestamptz(6)
  last_sign_in_at                                 DateTime?              @db.Timestamptz(6)
  raw_app_meta_data                               Json?
  raw_user_meta_data                              Json?
  is_super_admin                                  Boolean?
  created_at                                      DateTime?              @db.Timestamptz(6)
  updated_at                                      DateTime?              @db.Timestamptz(6)
  phone                                           String?                @unique
  phone_confirmed_at                              DateTime?              @db.Timestamptz(6)
  phone_change                                    String?                @default("")
  phone_change_token                              String?                @default("") @db.VarChar(255)
  phone_change_sent_at                            DateTime?              @db.Timestamptz(6)
  confirmed_at                                    DateTime?              @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current                      String?                @unique(map: "email_change_token_current_idx", where: raw("((email_change_token_current)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  email_change_confirm_status                     Int?                   @default(0) @db.SmallInt
  banned_until                                    DateTime?              @db.Timestamptz(6)
  reauthentication_token                          String?                @unique(map: "reauthentication_token_idx", where: raw("((reauthentication_token)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  reauthentication_sent_at                        DateTime?              @db.Timestamptz(6)
  is_sso_user                                     Boolean                @default(false)
  deleted_at                                      DateTime?              @db.Timestamptz(6)
  is_anonymous                                    Boolean                @default(false)
  identities                                      identities[]
  mfa_factors                                     mfa_factors[]
  oauth_authorizations                            oauth_authorizations[]
  oauth_consents                                  oauth_consents[]
  one_time_tokens                                 one_time_tokens[]
  sessions                                        sessions[]
  webauthn_challenges                             webauthn_challenges[]
  webauthn_credentials                            webauthn_credentials[]
  customer_group                                  customer_group[]
  customer_requests                               customer_requests[]
  customer_segment                                customer_segment[]
  customeruser                                    customeruser?
  deliveries                                      deliveries[]
  dhl_shipments                                   dhl_shipments[]
  noti_read_log                                   noti_read_log[]
  product_review_product_review_replied_byTousers product_review[]       @relation("product_review_replied_byTousers")
  product_review_product_review_user_idTousers    product_review[]       @relation("product_review_user_idTousers")
  user_favorites                                  user_favorites[]
  user_login_history                              user_login_history[]
  users                                           public_users?

  @@index([instance_id])
  @@index([is_anonymous])
  @@map("users")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model webauthn_challenges {
  id             String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?     @db.Uuid
  challenge_type String
  session_data   Json
  created_at     DateTime    @default(now()) @db.Timestamptz(6)
  expires_at     DateTime    @db.Timestamptz(6)
  users          auth_users? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at])
  @@index([user_id])
  @@schema("auth")
}

model webauthn_credentials {
  id               String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id          String     @db.Uuid
  credential_id    Bytes      @unique
  public_key       Bytes
  attestation_type String     @default("")
  aaguid           String?    @db.Uuid
  sign_count       BigInt     @default(0)
  transports       Json       @default("[]")
  backup_eligible  Boolean    @default(false)
  backed_up        Boolean    @default(false)
  friendly_name    String     @default("")
  created_at       DateTime   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime   @default(now()) @db.Timestamptz(6)
  last_used_at     DateTime?  @db.Timestamptz(6)
  users            auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id])
  @@schema("auth")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model CES {
  id           Int       @id @default(autoincrement())
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  process      String?
  job_id       String?
  job_type     String?
  job_create   String?
  job_status   String?
  note         String?
  packing_note String?
  goods        String?
  doc_no       String?
  status_time  DateTime? @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ConversationMemory {
  id         Int      @id @default(autoincrement())
  created_at DateTime @default(now()) @db.Timestamptz(6)
  message    String?
  recipient  String?
  sender     String?

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model IM {
  id                          Int                @id @default(autoincrement())
  created_at                  DateTime           @default(now()) @db.Timestamptz(6)
  docid                       String             @unique(map: "IM_io_id_key")
  customer                    String?
  note                        String?
  create_by                   String?            @db.Uuid
  status                      String?
  im                          String?
  customer_name               String?
  tel                         String?
  updated_at                  DateTime?          @default(now()) @db.Timestamptz(6)
  update_by                   String?
  history                     Json?
  type                        String?
  delivery_address_id         String?            @db.Uuid
  invoice_id                  Int?
  shipping_address            String?
  has_fifo_violation          Boolean?           @default(false)
  price_valid_days            String?
  delivery_days               String?
  payment_days                String?
  customeruser_id             String?            @db.Uuid
  contact_id                  Int?
  prefix                      String?
  need_smapprove              Boolean?           @default(false)
  sopt_id                     Int?
  buyer_customeruser_id       String?
  payment_status              String?
  log_payment                 DateTime?          @db.Timestamptz(6)
  order_uuid                  String?            @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_id                  Int?
  shipping_cost               Float?
  admin_verify_status         String?
  payment_invoice_no          String?
  credit_terms_day            Int?
  is_admin_order_created      Boolean?
  admin_updated_at            DateTime?          @db.Timestamptz(6)
  shipping_address_id         Int?
  order_created_by            String?            @db.Uuid
  payment_method_type         String?
  customer_invoice_address_id Int?
  contact                     contact?           @relation(fields: [contact_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                       public_users?      @relation(fields: [create_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser                customeruser?      @relation(fields: [customeruser_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_delivery           customer_delivery? @relation(fields: [delivery_address_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  invoicehead                 invoicehead?       @relation(fields: [invoice_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  sopt_head                   sopt_head?         @relation(fields: [sopt_id], references: [id], onDelete: Cascade)
  im_goods                    im_goods[]

  @@schema("public")
}

model IO {
  id                  Int                @id @default(autoincrement())
  created_at          DateTime           @default(now()) @db.Timestamptz(6)
  docid               String             @unique(map: "IO_po_id_key")
  customer            String?
  note                String?
  create_by           String?            @db.Uuid
  status              String?
  SR                  String?
  SX                  String?
  customer_name       String?
  tel                 String?
  updated_at          DateTime?          @default(now()) @db.Timestamptz(6)
  update_by           String?
  history             Json?
  delivery_address_id String?            @db.Uuid
  invoice_id          Int?
  users               public_users?      @relation(fields: [create_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_delivery   customer_delivery? @relation(fields: [delivery_address_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  head                head?              @relation(fields: [invoice_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model PO {
  id                        Int                @id @default(autoincrement())
  created_at                DateTime           @default(now()) @db.Timestamptz(6)
  docid                     String             @unique(map: "PO_po_id_key")
  customer                  String?
  note                      String?
  create_by                 String?            @db.Uuid
  status                    String?
  PQ                        String?
  PGT                       String?
  history                   Json?
  customer_name             String?
  tel                       String?
  updated_at                DateTime?          @default(now()) @db.Timestamptz(6)
  update_by                 String?            @db.Uuid
  delivery_address_id       String?            @db.Uuid
  invoice_id                Int?
  shipping_address          String?
  users_PO_create_byTousers public_users?      @relation("PO_create_byTousers", fields: [create_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_delivery         customer_delivery? @relation(fields: [delivery_address_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  invoicehead               invoicehead?       @relation(fields: [invoice_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users_PO_update_byTousers public_users?      @relation("PO_update_byTousers", fields: [update_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  po_goods                  po_goods[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Product {
  id         Int       @id @default(autoincrement())
  created_at DateTime? @db.Timestamptz(6)
  cat        String?
  mat        String?
  min_price  Float?

  @@schema("public")
}

model Productdata {
  id                                                            Int                      @id @default(autoincrement())
  created_at                                                    DateTime                 @default(now()) @db.Timestamptz(6)
  MM_RPTMAT_MATUnit_ID                                          String?                  @unique @map("MM_RPTMAT_MATUnit ID")
  MM_RPTMAT_MAT_ID                                              String?                  @unique @map("MM_RPTMAT_MAT ID")
  MM_RPTMAT_MAT_Name                                            String?                  @map("MM_RPTMAT_MAT Name")
  MM_RPTMAT_Displayed_Unit_Category                             String?                  @map("MM_RPTMAT_Displayed Unit Category")
  MM_RPTMAT_MAT_Category                                        String?                  @map("MM_RPTMAT_MAT Category")
  MM_RPTMAT_Status                                              String?
  MM_RPTMAT_Default_Unit_Size                                   String?                  @map("MM_RPTMAT_Default Unit Size")
  MM_RPTMAT_Qty_Unit                                            String?                  @map("MM_RPTMAT_Qty-Unit")
  MM_RPTMAT_AGQty_Unit                                          String?                  @map("MM_RPTMAT_AGQty-Unit")
  MM_RPTMAT_Description                                         String?
  priceonline                                                   Decimal?                 @db.Decimal
  detail                                                        String?
  tag                                                           String?
  productpic                                                    String?
  company_id                                                    Int?
  rich_description                                              String?
  brand                                                         Int?
  MM_RPTMAT_MAT_Type                                            Int?                     @map("MM_RPTMAT_MAT Type")
  onlinestatus                                                  Boolean?
  product_manager                                               String?                  @db.Uuid
  type_l2_id                                                    Int?
  type_l3_id                                                    Int?
  producttype                                                   producttype?             @relation(fields: [MM_RPTMAT_MAT_Type], references: [id], onDelete: NoAction, onUpdate: NoAction)
  brand_Productdata_brandTobrand                                brand?                   @relation("Productdata_brandTobrand", fields: [brand], references: [id], onDelete: NoAction, onUpdate: NoAction)
  companies                                                     public_companies?        @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                                                         public_users?            @relation(fields: [product_manager], references: [id], onDelete: NoAction, onUpdate: NoAction)
  producttype_level2                                            producttype_level2?      @relation(fields: [type_l2_id], references: [id], onUpdate: NoAction)
  producttype_level3                                            producttype_level3?      @relation(fields: [type_l3_id], references: [id], onUpdate: NoAction)
  liststock                                                     liststock?
  order_product                                                 order_product[]
  po_goods                                                      po_goods[]
  product_options                                               public_product_options[]
  product_sale                                                  product_sale[]
  promotion_gift_promotion_gift_gift_product_idToProductdata    promotion_gift[]         @relation("promotion_gift_gift_product_idToProductdata")
  promotion_gift_promotion_gift_trigger_product_idToProductdata promotion_gift[]         @relation("promotion_gift_trigger_product_idToProductdata")
  promotion_product                                             promotion_product[]

  @@schema("public")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model Saleperson {
  sharepoint_id    Int
  Person_ID        String? @map("Person ID")
  Name_Surname     String?
  department       String?
  linktopage       String?
  email            String?
  lineID           String?
  company          String?
  Planetone        String?
  PICPROFILE       String?
  MFPERSON         String?
  TEL              String?
  MFmail           String?
  page             String?
  position         String?
  id               Int     @default(autoincrement())
  team             String?
  emailsalesupport String?
  emailmaneger     String?
  avatar_url       String?
  tel              String?
  address          String?

  @@ignore
  @@schema("public")
}

model addgoods {
  id                          Int           @id @default(autoincrement())
  created_at                  DateTime      @default(now()) @db.Timestamptz(6)
  brand                       Int?
  matcatrequest               String?
  matnamerequest              String?
  matcat                      String?
  matname                     String?
  request_by                  String?       @db.Uuid
  status                      String?
  add_id                      String?
  brand_addgoods_brandTobrand brand?        @relation("addgoods_brandTobrand", fields: [brand], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                       public_users? @relation(fields: [request_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admin_activity_log {
  id          Int      @id @default(autoincrement())
  user_authid String?
  user_name   String?
  action      String
  resource    String
  resource_id String?
  details     Json?
  created_at  DateTime @default(now()) @db.Timestamptz(6)

  @@index([resource], map: "idx_admin_log_resource")
  @@index([created_at(sort: Desc)], map: "idx_admin_log_time")
  @@index([user_authid], map: "idx_admin_log_user")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_config {
  id             Int      @id @default(autoincrement())
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  latest_version String?
  force_update   Boolean?
  topic          String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_settings {
  key   String  @id
  value String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model app_theme_config {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  config_name           String    @unique @default("default")
  header_color          String?   @default("#343635")
  sub_header_color      String?   @default("#727073")
  selected_color        String?   @default("#3DBDC6")
  primary_color         String?   @default("#009688")
  primary_appbar_color  String?   @default("#6E4DF5")
  primary_light_color   String?   @default("#FFECDF")
  secondary_color       String?   @default("#979797")
  text_color            String?   @default("#000000")
  gradient_start_color  String?   @default("#FFA53E")
  gradient_end_color    String?   @default("#FF7643")
  animation_duration_ms Int?      @default(200)
  default_duration_ms   Int?      @default(250)
  heading_font_size     Decimal?  @default(24.0) @db.Decimal
  heading_font_weight   String?   @default("bold")
  border_radius         Decimal?  @default(16.0) @db.Decimal
  is_active             Boolean?  @default(true)
  created_at            DateTime? @default(now()) @db.Timestamptz(6)
  updated_at            DateTime? @default(now()) @db.Timestamptz(6)
  updated_by            String?
  alert_color           String?   @default("#F44336")

  @@index([is_active], map: "idx_active_theme", where: raw("(is_active = true)"))
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model asset_check {
  id              Int           @id @default(autoincrement())
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  assett_goods_id Int?
  instrument_pic  String?
  sn_pic          String?
  latitude        String?
  longtitude      String?
  recorded_by     String?       @db.Uuid
  status          String?
  note            String?
  assett_goods    assett_goods? @relation(fields: [assett_goods_id], references: [id], onDelete: Cascade)
  users           public_users? @relation(fields: [recorded_by], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assett {
  id                                 Int                 @id(map: "assett_instrument_pkey") @default(autoincrement())
  created_at                         DateTime            @default(now()) @db.Timestamptz(6)
  customeruser_id                    String?             @db.Uuid
  contact_id                         Int?
  install                            String?
  install_place                      String?
  installer                          String?             @db.Uuid
  recorded_by                        String?             @db.Uuid
  sale_person_id                     String?             @db.Uuid
  ref_doc                            String?
  note                               String?
  doc_id                             String?
  companies_id                       Int?
  status                             String?
  log                                String?
  action_date                        DateTime?           @db.Timestamptz(6)
  type                               String?
  users_assett_installerTousers      public_users?       @relation("assett_installerTousers", fields: [installer], references: [id], onDelete: Cascade)
  companies                          public_companies?   @relation(fields: [companies_id], references: [id], onDelete: Cascade, map: "assett_instrument_companies_id_fkey")
  contact                            contact?            @relation(fields: [contact_id], references: [id], onDelete: Cascade, map: "assett_instrument_contact_id_fkey")
  customeruser                       customeruser?       @relation(fields: [customeruser_id], references: [id], onDelete: Cascade, map: "assett_instrument_customeruser_id_fkey")
  users_assett_recorded_byTousers    public_users?       @relation("assett_recorded_byTousers", fields: [recorded_by], references: [id], onDelete: Cascade, map: "assett_instrument_recorded_by_fkey")
  users_assett_sale_person_idTousers public_users?       @relation("assett_sale_person_idTousers", fields: [sale_person_id], references: [id], onDelete: Cascade, map: "assett_instrument_sale_person_id_fkey")
  assett_goods_docs                  assett_goods_docs[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assett_goods {
  id                Int                 @id @default(autoincrement())
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  sn_no             String?
  catalog_no        String?
  name              String?
  type              String?
  cost              Decimal?            @db.Decimal
  product_gr        String?
  brand             String?
  depreciation      Int?
  size              String?
  asset_check       asset_check[]
  assett_goods_docs assett_goods_docs[]
  assett_waranty    assett_waranty[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assett_waranty {
  id             Int           @id @default(autoincrement())
  created_at     DateTime      @default(now()) @db.Timestamptz(6)
  asset_goods_id Int?
  type           String?
  start_date     DateTime?     @db.Timestamptz(6)
  end_date       DateTime?     @db.Timestamptz(6)
  pm_count       Int?
  note           String?
  price          Decimal?      @db.Decimal
  recorded_by    String?       @db.Uuid
  status         String?
  assett_goods   assett_goods? @relation(fields: [asset_goods_id], references: [id], onDelete: Cascade)
  users          public_users? @relation(fields: [recorded_by], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_attribute_groups {
  id                     Int                 @id @default(autoincrement())
  attr_group_name        String?
  attr_group_description String?
  is_active              Boolean?
  created_at             DateTime?           @db.Timestamptz(6)
  updated_at             DateTime?           @db.Timestamptz(6)
  companies_id           Int?
  division_id            Int?
  companies              public_companies?   @relation(fields: [companies_id], references: [id], onDelete: Cascade)
  departments            departments?        @relation(fields: [division_id], references: [id], onDelete: Cascade)
  attributes             public_attributes[]

  @@map("attribute_groups")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_attributes {
  id               Int                     @id @default(autoincrement())
  attr_group_id    Int
  attr_name        String?
  attr_description String?
  is_active        Boolean?
  created_at       DateTime?               @db.Timestamptz(6)
  updated_at       DateTime?               @db.Timestamptz(6)
  attribute_groups public_attribute_groups @relation(fields: [attr_group_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sale_target_attr sale_target_attr[]

  @@map("attributes")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model auto_reply_rules {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  platform     String    @default("all")
  match_type   String    @default("contains")
  keyword      String
  reply        String
  active       Boolean   @default(true)
  priority     Int       @default(99)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  days_of_week Int[]
  time_start   DateTime? @db.Time(6)
  time_end     DateTime? @db.Time(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model benefit {
  id         Int      @id @default(autoincrement())
  created_at DateTime @default(now()) @db.Timestamptz(6)
  tier       String?
  benefit    String?
  icon       String?
  status     Boolean?
  detail     String?

  @@schema("public")
}

model bill_tracking {
  Person_ID          String? @map("Person ID")
  Name_Surname       String? @map("Name Surname")
  dateprocess        String?
  Sx_No_             String? @map("Sx No.")
  Process            String?
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // สร้างเมื่อ DateTime? @default(now()) @map("สร้างเมื่อ") @db.Timestamptz(6)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  note               String?
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // สิ่งที่แนบมา String? @map("สิ่งที่แนบมา")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  shipby             String?
  outsource_no       String?
  ID                 Int     @id(map: "bill tracking_pkey") @default(autoincrement())
  CUSTOMEREMAIL      String?
  company            String?
  delivery_condition String?
  email              String?
  enroll_date        String? @map("enroll date")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // พนักงานขาย String? @map("พนักงานขาย")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  REFDOC             String?
  TEL                String?
  SR                 String?
  load               String? @default("no")
  document_images    String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model biodesignsale {
  ID                 Int      @default(autoincrement())
  DATE               String?
  DOC_ID             String   @map("DOC ID")
  C__REF             String?  @map("C. REF")
  CUSTOMER           String?
  C__BUYER           String?  @map("C. BUYER")
  C__PAYMENT         String?  @map("C. PAYMENT")
  SO_TYPE            String?  @map("SO TYPE")
  MAT_CAT            String?  @map("MAT CAT")
  MAT_ID             String   @map("MAT ID")
  MAT                String?
  UNIT               String?
  Printing_Note      String?  @map("Printing Note")
  STXI_Printing_Note String?  @map("STXI Printing Note")
  PERSONNEL          String?
  UNIT_PRICE         Decimal? @map("UNIT PRICE") @db.Decimal
  R_QTY              Decimal? @map("R-QTY") @db.Decimal
  AG_QTY             Decimal? @map("AG-QTY") @db.Decimal
  AMOUNT             Decimal  @db.Decimal
  COST               Decimal  @db.Decimal
  PROFIT_LOSS        Float?   @map("PROFIT/LOSS")
  P_L                Float?   @map("% P/L")

  @@id([DOC_ID, MAT_ID, AMOUNT, COST])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model biodesignstock {
  ID                  Int     @id @default(autoincrement())
  MATUnit             String?
  PO_                 String? @map("PO#")
  PO_Date             String? @map("PO Date")
  PO_REF              String? @map("PO REF")
  Purchaser_Personnel String? @map("Purchaser Personnel")
  PR_                 String? @map("PR#")
  Lot                 String?
  MAT_IN              String? @map("MAT IN")
  MAT_IN_Ref          String? @map("MAT IN Ref")
  INV_ID_MATIN_       String? @map("INV ID(MATIN)")
  Received_Date_Time  String? @map("Received Date Time")
  Location_ID         String? @map("Location ID")
  Unit_Cost           Float?  @map("Unit Cost")
  Cost_BLN            Float?  @map("Cost BLN")
  QTY_BLN_1_          Int?    @map("QTY BLN(1)")
  QTY_Ready_2_        Int?    @map("QTY Ready(2)")
  QTY_PN_Out_3_       Int?    @map("QTY PN Out(3)")
  QTY_1_2             Int?    @map("QTY 1+2")
  QTY_1_2_3           Int?    @map("QTY 1+2-3")
  QTY_1_3             Int?    @map("QTY 1-3")
  Unit                String?
  Age                 Int?
  Expired             String?
  Avail__Life         Int?    @map("Avail. Life")
  Product_Life        String? @map("Product Life")

  @@schema("public")
}

model brand {
  id                                   Int           @id @default(autoincrement())
  created_at                           DateTime      @default(now()) @db.Timestamptz(6)
  brand                                String?
  active                               Boolean?
  country                              String?
  pic                                  String?
  Productdata_Productdata_brandTobrand Productdata[] @relation("Productdata_brandTobrand")
  addgoods_addgoods_brandTobrand       addgoods[]    @relation("addgoods_brandTobrand")
  matcat_matcat_brandTobrand           matcat[]      @relation("matcat_brandTobrand")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_brands {
  id          Int               @id @default(autoincrement())
  brand_image String?
  url_image   String?
  brand_name  String?
  country_id  Int?
  is_active   Boolean?
  created_at  DateTime?         @db.Timestamptz(6)
  updated_at  DateTime?         @db.Timestamptz(6)
  countries   public_countries? @relation(fields: [country_id], references: [id], onDelete: Cascade)
  products    public_products[]

  @@map("brands")
  @@schema("public")
}

model calls {
  id             String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  caller_id      String?
  receiver_id    String?
  sdp_offer      String?
  sdp_answer     String?
  ice_candidates Json?
  status         String? @default("ringing")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ces_job {
  id         String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at DateTime     @default(now()) @db.Timestamptz(6)
  type       String?
  doc_no     String?
  created_by String?
  ref        String?
  ces_status ces_status[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ces_status {
  id         Int           @id @default(autoincrement())
  created_at DateTime      @default(now()) @db.Timestamptz(6)
  ces_job_id String?       @db.Uuid
  status     String?
  note       String?
  created_by String?       @db.Uuid
  ces_job    ces_job?      @relation(fields: [ces_job_id], references: [id], onDelete: Cascade)
  users      public_users? @relation(fields: [created_by], references: [authid], onDelete: SetDefault)

  @@schema("public")
}

model chat_participants {
  id           String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  chat_room_id String?       @db.Uuid
  user_id      String?       @db.Uuid
  joined_at    DateTime?     @default(now()) @db.Timestamp(6)
  chat_rooms   chat_rooms?   @relation(fields: [chat_room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users        public_users? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([chat_room_id, user_id])
  @@schema("public")
}

model chat_rooms {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name              String?
  is_group          Boolean?            @default(false)
  created_at        DateTime?           @default(now()) @db.Timestamp(6)
  chat_participants chat_participants[]
  hidden_chats      hidden_chats[]
  messages          messages[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model claim_requests {
  id                String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id           String       @db.Uuid
  invoice_no        String
  amount            Decimal      @db.Decimal
  invoice_date      DateTime?    @db.Timestamptz(6)
  note              String?
  receipt_image_url String?
  status            String       @default("pending")
  created_at        DateTime     @default(now()) @db.Timestamptz(6)
  updated_at        DateTime     @default(now()) @db.Timestamptz(6)
  log               Json?        @default("[]")
  updated_by        String?
  customeruser      customeruser @relation(fields: [user_id], references: [authid], onDelete: NoAction, onUpdate: NoAction, map: "claim_requests_user_id_fkey2")

  @@schema("public")
}

model public_companies {
  id               Int                       @id @default(autoincrement())
  created_at       DateTime                  @default(now()) @db.Timestamptz(6)
  name             String?
  active           Boolean?                  @default(true)
  companypic       String?
  short            String?
  company_image    String?
  url_image        String?
  Productdata      Productdata[]
  assett           assett[]
  attribute_groups public_attribute_groups[]
  departments      departments[]
  process_options  process_options[]
  product_target   product_target[]
  products         public_products[]
  sale_target      sale_target[]
  users            public_users[]

  @@map("companies")
  @@schema("public")
}

model contact {
  id                 Int                 @id @default(autoincrement())
  created_at         DateTime            @default(now()) @db.Timestamptz(6)
  namesurname        String?
  company            String?
  tel                String?
  note               String?
  address1           String?
  address2           String?
  addresssubdestrict String?
  addressdestrict    String?
  addressprovince    String?
  addresspostel      String?
  create_by          String?             @db.Uuid
  prefix             String?
  email              String?
  division           String?
  IM                 IM[]
  assett             assett[]
  users              public_users?       @relation(fields: [create_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_delivery  customer_delivery[]
  customer_invoice   customer_invoice[]
  sopt_head          sopt_head[]

  @@schema("public")
}

model content_types {
  id           String    @id
  name         String
  display_name String
  icon         String?
  color        String?
  description  String?
  active       Boolean?  @default(true)
  sort_order   Int?      @default(0)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_countries {
  id           Int             @id @default(autoincrement())
  country_name String?
  created_at   DateTime?       @db.Timestamptz(6)
  updated_at   DateTime?       @db.Timestamptz(6)
  is_active    Boolean?
  short_name   String?
  brands       public_brands[]

  @@map("countries")
  @@schema("public")
}

model customer_delivery {
  id             String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at     DateTime      @default(now()) @db.Timestamptz(6)
  customer_id    String?       @db.Uuid
  tel            String?
  address1       String?
  address2       String?
  subdestrict    String?
  destrict       String?
  province       String?
  postel         String?
  contact_id     Int?
  recerived_name String?
  IM             IM[]
  IO             IO[]
  PO             PO[]
  contact        contact?      @relation(fields: [contact_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser   customeruser? @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_events {
  id                Int                 @id @default(autoincrement())
  name              String
  start_date        DateTime            @db.Date
  end_date          DateTime            @db.Date
  active            Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  customer_requests customer_requests[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_group {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                  String
  description           String?
  color                 String                  @default("#6366F1")
  member_count          Int                     @default(0)
  created_at            DateTime                @default(now()) @db.Timestamptz(6)
  updated_at            DateTime                @default(now()) @db.Timestamptz(6)
  created_by            String?                 @db.Uuid
  group_type            String                  @default("permanent")
  filter_criteria       Json?
  condition_logic       String                  @default("and")
  conditions            Json?
  users                 auth_users?             @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  customer_group_member customer_group_member[]
  customernoti          customernoti[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_group_member {
  group_id        String         @db.Uuid
  customeruser_id String
  added_at        DateTime       @default(now()) @db.Timestamptz(6)
  id              Int            @id @default(autoincrement())
  customer_group  customer_group @relation(fields: [group_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([group_id, customeruser_id], map: "customer_group_member_group_customeruser_unique")
  @@index([customeruser_id], map: "cgm_customer_idx")
  @@schema("public")
}

model customer_invoice {
  id              Int           @id(map: "customer_invoices_pkey") @default(autoincrement())
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  invoice_id      Int?
  customeruser_id String?       @db.Uuid
  contact_id      Int?
  contact         contact?      @relation(fields: [contact_id], references: [id], onDelete: Cascade)
  customeruser    customeruser? @relation(fields: [customeruser_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  invoicehead     invoicehead?  @relation(fields: [invoice_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customer_invoices_invoice_id_fkey")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_requests {
  id                 Int              @id @default(autoincrement())
  customeruser_id    String?          @db.Uuid
  name               String?
  tel                String?
  company            String?
  event_id           Int?
  request            String?
  created_by         String?          @db.Uuid
  created_at         DateTime         @default(now()) @db.Timestamptz(6)
  assigned_users_id  String?          @db.Uuid
  sopt_head_id       Int?
  assigned_users_ids String[]         @default([]) @db.Uuid
  sopt_head_ids      Int[]            @default([])
  email              String?
  has_data           Boolean?
  is_active          Boolean          @default(true)
  public_users       public_users?    @relation(fields: [assigned_users_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  auth_users         auth_users?      @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser       customeruser?    @relation(fields: [customeruser_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_events    customer_events? @relation(fields: [event_id], references: [id], onDelete: Cascade)
  sopt_head          sopt_head?       @relation(fields: [sopt_head_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_saved_cards {
  id            String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customer_id   String       @db.Uuid
  card_type     String
  masked_number String
  is_default    Boolean      @default(false)
  created_at    DateTime     @default(now()) @db.Timestamptz(6)
  customeruser  customeruser @relation(fields: [customer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([customer_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_segment {
  id           String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name         String
  description  String?
  conditions   Json           @default("[]")
  member_count Int            @default(0)
  created_by   String?        @db.Uuid
  created_at   DateTime       @default(now()) @db.Timestamptz(6)
  updated_at   DateTime       @default(now()) @db.Timestamptz(6)
  users        auth_users?    @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  customernoti customernoti[]
  popup_image  popup_image[]

  @@index([updated_at(sort: Desc)], map: "idx_customer_segment_updated")
  @@schema("public")
}

model customer_support_messages {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customer_auth_id String    @db.Uuid
  customer_id      String?
  customer_name    String?
  message          String
  sender           String    @default("customer")
  admin_user_id    String?
  admin_name       String?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  read_by_admin    Boolean?  @default(false)
  read_by_customer Boolean?  @default(false)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_tax_invoices {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customer_id    String       @db.Uuid
  type           String
  name           String
  tax_id         String
  email          String
  phone          String?
  address        String
  postal_code    String?
  is_head_office Boolean?
  is_default     Boolean      @default(false)
  created_at     DateTime     @default(now()) @db.Timestamptz(6)
  customeruser   customeruser @relation(fields: [customer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([customer_id])
  @@schema("public")
}

model customer_tier {
  id           String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id      String?       @unique @db.Uuid
  tier         String        @db.VarChar(20)
  start_at     DateTime      @default(now()) @db.Timestamptz(6)
  expired_at   DateTime      @db.Timestamptz(6)
  customeruser customeruser? @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model customerevent {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  event_id        String?       @db.Uuid
  customeruser_id String?       @db.Uuid
  registerdate    DateTime?     @db.Timestamptz(6)
  attendevent     DateTime?     @db.Timestamptz(6)
  type            String?
  checked_in      Boolean?      @default(false)
  checkin_at      DateTime?     @db.Timestamptz(6)
  qr_token        String?       @unique(map: "idx_customerevent_qr_token") @default(dbgenerated("(gen_random_uuid())::text"))
  customeruser    customeruser? @relation(fields: [customeruser_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  email_logs      email_logs[]

  @@index([event_id], map: "idx_customerevent_event_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model customernoti {
  id                                    Int                  @id @default(autoincrement())
  created_at                            DateTime             @default(now()) @db.Timestamptz(6)
  title                                 String
  message                               String?
  senddate                              DateTime             @db.Timestamptz(6)
  tier                                  String?
  status                                String?              @default("draft")
  sent_at                               DateTime?            @db.Timestamptz(6)
  navigation                            String?
  age_range                             String?
  province                              String?
  specific_name                         String?
  send_on_birthday                      Boolean?
  weblink                               String?
  image                                 String?
  voucher_id                            Int?
  target_type                           String?              @default("tier")
  target_auth_ids                       String[]
  sent_count                            Int?                 @default(0)
  target_promotion_id                   Int?
  target_event_id                       String?
  target_seminar_dwid                   String?
  target_seminar_company                String?
  send_channels                         String[]             @default(["app"])
  segment_id                            String?              @db.Uuid
  target_customer_group_id              String?              @db.Uuid
  targeting_logic                       String               @default("and")
  click_tag_id                          String?              @db.Uuid
  target_tag_id                         String?              @db.Uuid
  target_tag_mode                       String?              @default("current")
  target_tag_date_from                  DateTime?            @db.Date
  target_tag_date_to                    DateTime?            @db.Date
  click_tag_ids                         Json?                @default("[]")
  send_mode                             String               @default("immediate")
  tags_customernoti_click_tag_idTotags  tags?                @relation("customernoti_click_tag_idTotags", fields: [click_tag_id], references: [id], onUpdate: NoAction)
  customer_segment                      customer_segment?    @relation(fields: [segment_id], references: [id], onUpdate: NoAction)
  customer_group                        customer_group?      @relation(fields: [target_customer_group_id], references: [id], onUpdate: NoAction)
  tags_customernoti_target_tag_idTotags tags?                @relation("customernoti_target_tag_idTotags", fields: [target_tag_id], references: [id], onUpdate: NoAction)
  voucher                               voucher?             @relation(fields: [voucher_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  noti_auto_send_log                    noti_auto_send_log[]
  noti_channel_stats                    noti_channel_stats?
  noti_read_log                         noti_read_log[]

  @@schema("public")
}

model customerseminar {
  id              Int          @id @default(autoincrement())
  customeruser_id String       @db.Uuid
  seminar_uuid    String       @db.Uuid
  seminar_dwid    String
  registered_at   DateTime?    @default(now()) @db.Timestamp(6)
  status          String?      @default("registered")
  company         String?
  customeruser    customeruser @relation(fields: [customeruser_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([customeruser_id, seminar_dwid])
  @@index([customeruser_id], map: "idx_customerseminar_user")
  @@index([seminar_uuid], map: "idx_customerseminar_uuid")
  @@schema("public")
}

model customeruser {
  id                        String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  authid                    String?                   @unique @default(dbgenerated("auth.uid()")) @db.Uuid
  member_id                 String?                   @unique(map: "customeruser_idno_key")
  created_at                DateTime                  @default(now()) @db.Timestamptz(6)
  prefix                    String?
  namesurname               String?
  email                     String?
  tel                       String?
  pdpa_accepted             Boolean?
  pdpa_accepted_at          DateTime?                 @db.Timestamptz(6)
  pdpa_version              String?
  gender                    String?
  birthday                  DateTime?                 @db.Date
  home_address_line1        String?
  home_address_line2        String?
  home_sub_district         String?
  home_district             String?
  home_province             String?
  home_postal_code          String?
  w1_address_line1          String?
  w1_address_line2          String?
  w1_sub_district           String?
  w1_district               String?
  w1_province               String?
  w1_postal_code            String?
  position                  String?
  w1_name                   String?
  status                    String?
  note                      String?
  Preferred_Contact_Channel String?
  fcm_token                 String?
  avatar_url                String?
  line_id                   String?
  is_online                 Boolean?
  last_online               String?
  picprofile                String?
  wtel                      String?
  gibthailink               Json?
  biodesignlink             Json?
  lableaderlink             Json?
  aeclink                   Json?
  doctorcallink             Json?
  miclink                   Json?
  log                       String?
  department                String?
  knowform                  String?
  referred_by               String?
  uninstalled_at            DateTime?                 @db.Timestamptz(6)
  line_user_id              String?
  facebook_psid             String?
  source                    String?
  tel2                      String?
  referral_campaign_id      Int?
  IM                        IM[]
  assett                    assett[]
  claim_requests            claim_requests[]
  customer_delivery         customer_delivery[]
  customer_invoice          customer_invoice[]
  customer_line_accounts    customer_line_accounts[]
  customer_requests         customer_requests[]
  customer_saved_cards      customer_saved_cards[]
  customer_tags             customer_tags[]
  customer_tax_invoices     customer_tax_invoices[]
  customer_tier             customer_tier?
  customerevent             customerevent[]
  customerseminar           customerseminar[]
  users                     auth_users?               @relation(fields: [authid], references: [id], onDelete: NoAction, onUpdate: NoAction)
  referral_settings         referral_settings?        @relation(fields: [referral_campaign_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser_users        customeruser_users[]
  customervoucher           customervoucher[]
  email_logs                email_logs[]
  noti_auto_send_log        noti_auto_send_log[]
  notification_logs         notification_logs[]
  order                     order[]
  promotion_claim_history   promotion_claim_history[]
  reward_points             reward_points[]
  reward_redemptions        reward_redemptions[]
  sopt_head                 sopt_head[]
  survey_responses          survey_responses[]
  user_reading_progress     user_reading_progress[]

  @@index([facebook_psid], map: "idx_customeruser_facebook_psid", where: raw("(facebook_psid IS NOT NULL)"))
  @@schema("public")
}

model customeruser_users {
  id              Int           @id(map: "customeruser_user_pkey") @default(autoincrement())
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  customeruser_id String?       @db.Uuid
  users_id        String?       @db.Uuid
  customeruser    customeruser? @relation(fields: [customeruser_id], references: [id], onDelete: Cascade, map: "customeruser_user_customeruser_id_fkey")
  users           public_users? @relation(fields: [users_id], references: [id], onDelete: Cascade, map: "customeruser_user_users_id_fkey")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customervoucher {
  id           Int           @id @default(autoincrement())
  created_at   DateTime      @default(now()) @db.Timestamptz(6)
  customer_id  String?       @db.Uuid
  voucher_id   Int?
  used         Boolean?      @default(false)
  exp          DateTime?     @db.Date
  use_detail   String?
  used_at      DateTime?     @db.Date
  voucherid    String        @unique
  customeruser customeruser? @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  voucher      voucher?      @relation(fields: [voucher_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

model deletion_requests {
  id         Int      @id @default(autoincrement())
  created_at DateTime @default(now()) @db.Timestamptz(6)
  user_id    String?  @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email      String?
  status     String?
  reason     String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model deliveries {
  id               Int               @id @default(autoincrement())
  event            String
  timestamp        DateTime          @db.Timestamptz(6)
  source           String?
  delivered_by     String?           @db.Uuid
  note             String?
  total_items      Int?
  total_quantity   Decimal?          @db.Decimal
  order_count      Int?
  created_at       DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?         @default(now()) @db.Timestamptz(6)
  customer_name    String?
  customer_phone   String?
  customer_address String?
  IO               String?
  users            auth_users?       @relation(fields: [delivered_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  delivery_items   delivery_items[]
  delivery_orders  delivery_orders[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model delivery_items {
  id           Int        @id @default(autoincrement())
  delivery_id  Int
  product_code String
  product_name String?
  mr           String?
  location     String?
  quantity     Decimal?   @db.Decimal
  unit         String?
  created_at   DateTime?  @default(now()) @db.Timestamptz(6)
  deliveries   deliveries @relation(fields: [delivery_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model delivery_orders {
  id            Int        @id @default(autoincrement())
  delivery_id   Int
  order_id      String
  customer_name String?
  product       String?
  created_at    DateTime?  @default(now()) @db.Timestamptz(6)
  deliveries    deliveries @relation(fields: [delivery_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model departments {
  id               Int                       @id @default(autoincrement())
  created_at       DateTime                  @default(now()) @db.Timestamptz(6)
  name             String?
  active           Boolean?                  @default(true)
  company          Int?
  attribute_groups public_attribute_groups[]
  companies        public_companies?         @relation(fields: [company], references: [id], onDelete: NoAction, onUpdate: NoAction)
  process_options  process_options[]
  sopt_status_data sopt_status_data[]
  teams            teams[]
  users            public_users[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dhl_shipments {
  id                 String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at         DateTime    @default(now()) @db.Timestamptz(6)
  created_by         String?     @db.Uuid
  sx_no              String
  tracking_number    String
  consignee_name     String
  consignee_company  String?
  consignee_address  String?
  consignee_district String?
  consignee_city     String?
  consignee_state    String?
  consignee_postcode String?
  consignee_phone    String?
  weight_kg          Decimal?    @db.Decimal
  cod_amount         Decimal?    @db.Decimal
  packing_items      Json        @default("[]")
  pickup_account_id  String?
  users              auth_users? @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_by, created_at(sort: Desc)], map: "dhl_shipments_created_by_idx")
  @@schema("public")
}

model documents {
  id        Int                    @id @default(autoincrement())
  content   String?
  metadata  Json?
  embedding Unsupported("vector")?

  @@schema("public")
}

model email_logs {
  id               String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customeruser_id  String?        @db.Uuid
  customerevent_id String?        @db.Uuid
  email_type       String
  recipient_email  String
  subject          String?
  status           String?        @default("sent")
  message_id       String?
  error_message    String?
  sent_at          DateTime?      @default(now()) @db.Timestamptz(6)
  metadata         Json?
  customerevent    customerevent? @relation(fields: [customerevent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser     customeruser?  @relation(fields: [customeruser_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([customerevent_id], map: "idx_email_logs_customerevent")
  @@index([customeruser_id], map: "idx_email_logs_customeruser")
  @@index([status], map: "idx_email_logs_status")
  @@index([email_type], map: "idx_email_logs_type")
  @@schema("public")
}

model email_resend_logs {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  redemption_id      String             @db.Uuid
  user_id            String?            @db.Uuid
  action_taken       String
  created_at         DateTime?          @default(now()) @db.Timestamptz(6)
  reward_redemptions reward_redemptions @relation(fields: [redemption_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model emailpreregister {
  id         Int      @id @default(autoincrement())
  created_at DateTime @default(now()) @db.Timestamptz(6)
  email      String?
  upload     Boolean  @default(false)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model public_events {
  id                 String        @id(map: "event_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at         DateTime      @default(now()) @db.Timestamptz(6)
  eventname          String?
  event_detail       String?
  event_registerdate String?
  event_preregister  String?
  eventStartDate     String?
  event_pic          String?
  location           String?
  category           String?
  tierregister       String?
  tierpreregister    String?
  attendee           Decimal?      @db.Decimal
  eventEndDate       String?
  link               String?
  event_category_id  Int?
  is_active          Boolean?
  popup_image        popup_image[]

  @@map("events")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model gibstock {
  id                  Int     @id @default(autoincrement())
  MATUnit             String?
  PO_                 String? @map("PO#")
  PO_Date             String? @map("PO Date")
  PO_REF              String? @map("PO REF")
  Purchaser_Personnel String? @map("Purchaser Personnel")
  PR_                 String? @map("PR#")
  Lot                 String?
  MAT_IN              String? @map("MAT IN")
  MAT_IN_Ref          String? @map("MAT IN Ref")
  INV_ID_MATIN_       String? @map("INV ID(MATIN)")
  Received_Date_Time  String? @map("Received Date Time")
  Location_ID         String? @map("Location ID")
  Unit_Cost           Float?  @map("Unit Cost")
  Cost_BLN            Float?  @map("Cost BLN")
  QTY_BLN_1_          Int?    @map("QTY BLN(1)")
  QTY_Ready_2_        Int?    @map("QTY Ready(2)")
  QTY_PN_Out_3_       Int?    @map("QTY PN Out(3)")
  QTY_1_2             Int?    @map("QTY 1+2")
  QTY_1_2_3           Int?    @map("QTY 1+2-3")
  QTY_1_3             Int?    @map("QTY 1-3")
  Unit                String?
  Age                 Int?
  Expired             String?
  Avail__Life         Int?    @map("Avail. Life")
  Product_Life        String? @map("Product Life")

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model gibthaisale {
  id                 Int      @id @default(autoincrement())
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  DATE               String?
  DOC_ID             String?  @map("DOC ID")
  C__REF             String?  @map("C. REF")
  CUSTOMER           String?
  C__BUYER           String?  @map("C. BUYER")
  SO_TYPE            String?  @map("SO TYPE")
  MAT_CAT            String?  @map("MAT CAT")
  MAT_ID             String?  @map("MAT ID")
  MAT                String?
  UNIT               String?
  Printing_Note      String?  @map("Printing Note")
  STXI_Printing_Note String?  @map("STXI Printing Note")
  PERSONNEL          String?
  UNIT_PRICE         Decimal? @map("UNIT PRICE") @db.Decimal
  R_QTY              Decimal? @map("R-QTY") @db.Decimal
  AG_QTY             Decimal? @map("AG-QTY") @db.Decimal
  AMOUNT             Decimal? @db.Decimal
  COST               Decimal? @db.Decimal
  PROFIT_LOSS        Decimal? @map("PROFIT/LOSS") @db.Decimal
  P_L                Decimal? @map("% P/L") @db.Decimal
  C__PAYMENT         String?  @map("C. PAYMENT")
  link               String?

  @@unique([DOC_ID, MAT_ID, AMOUNT, COST], map: "unique_invoice_item")
  @@schema("public")
}

model head {
  id         Int       @id @default(autoincrement())
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  name       String?
  start      DateTime? @db.Date
  end        String?
  pichead    String?
  picfull    String?
  detail     String?
  page       String?
  link       String?
  IO         IO[]

  @@schema("public")
}

model hidden_chats {
  id           String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id      String?       @db.Uuid
  chat_room_id String?       @db.Uuid
  hidden_at    DateTime?     @default(now()) @db.Timestamp(6)
  chat_rooms   chat_rooms?   @relation(fields: [chat_room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users        public_users? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model holiday {
  id         Int       @id @default(autoincrement())
  created_at DateTime  @default(now()) @db.Timestamptz(6)
  date       DateTime? @db.Date
  event      String?

  @@schema("public")
}

model im_goods {
  id                                   Int           @id @default(autoincrement())
  created_at                           DateTime      @default(now()) @db.Timestamptz(6)
  docid                                Int
  mat_cat                              String?
  mat_name                             String?
  qty                                  Int
  brand                                String?
  reason                               String?
  im_qty                               Int?
  itemstatus                           String?       @default("waiting")
  MR                                   String?
  location                             String?
  Owner                                String?
  exp                                  DateTime?     @db.Date
  lot                                  String?
  needs_pm_approval                    Boolean?      @default(false)
  approved_by_pm                       String?       @db.Uuid
  pm_approved_at                       DateTime?     @db.Timestamptz(6)
  pm_note                              String?
  price                                Float?
  category                             String?
  po_ref                               String?
  rsv_ref                              String?
  name_edit                            String?
  io_qty                               Int?
  need_smapprove                       Boolean?
  users_id                             String?       @db.Uuid
  sn                                   String?
  product_option_id                    Int?
  sale_price                           Float?
  order_price                          Float?
  admin_updated_at                     DateTime?     @db.Timestamptz(6)
  is_free                              Boolean?
  promotion_from_product_option_id     Int?
  admin_updated_by                     String?       @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users_im_goods_approved_by_pmTousers public_users? @relation("im_goods_approved_by_pmTousers", fields: [approved_by_pm], references: [id], onDelete: NoAction, onUpdate: NoAction)
  IM                                   IM            @relation(fields: [docid], references: [id], onDelete: Cascade)
  users_im_goods_users_idTousers       public_users? @relation("im_goods_users_idTousers", fields: [users_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

model invoicehead {
  id                       Int                        @id @default(autoincrement())
  created_at               DateTime                   @default(now()) @db.Timestamptz(6)
  company_name             String?                    @unique
  Entity_ID                String                     @unique @map("Entity ID")
  Entity_Name              String?                    @map("Entity Name")
  Address1                 String?
  Address2                 String?
  Province                 String?
  Country                  String?
  PostCode                 String?
  Tax_Number               String?                    @map("Tax Number")
  Branch_Tax_Name          String?                    @map("Branch Tax Name")
  Branch_Tax_ID            String?                    @map("Branch Tax ID")
  customer_invoice_address customer_invoice_address[]
  IM                       IM[]
  PO                       PO[]
  customer_invoice         customer_invoice[]

  @@schema("public")
}

model io_goods {
  id         Int           @id @default(autoincrement())
  created_at DateTime      @default(now()) @db.Timestamptz(6)
  docid      Int
  mat_cat    String?
  mat_name   String?
  qty        Int
  price      Decimal       @db.Decimal
  brand      String?
  reason     String?
  io_qty     Decimal?      @db.Decimal
  itemstatus String?       @default("waiting")
  users_id   String?       @db.Uuid
  users      public_users? @relation(fields: [users_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

model ioplanet {
  Date           String?
  INV_ID         String? @map("INV ID")
  Source         String?
  Ref_ID         String? @map("Ref ID")
  PSN_Sales      String? @map("PSN Sales")
  Entity         String?
  WH_IN          String? @map("WH IN")
  WH_OUT         String? @map("WH OUT")
  MATUnitID      String?
  MATUnit        String?
  Lot            String?
  Quantity       Int?
  Unit_Price_STX String? @map("Unit Price STX")
  Amount_STX     String? @map("Amount STX")
  id             Int     @id @unique @default(autoincrement())

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lableadersale {
  ID                 Int      @default(autoincrement())
  DATE               String?
  DOC_ID             String   @map("DOC ID")
  C__REF             String?  @map("C. REF")
  CUSTOMER           String?
  C__BUYER           String?  @map("C. BUYER")
  C__PAYMENT         String?  @map("C. PAYMENT")
  SO_TYPE            String?  @map("SO TYPE")
  MAT_CAT            String?  @map("MAT CAT")
  MAT_ID             String   @map("MAT ID")
  MAT                String?
  UNIT               String?
  Printing_Note      String?  @map("Printing Note")
  STXI_Printing_Note String?  @map("STXI Printing Note")
  PERSONNEL          String?
  UNIT_PRICE         Decimal? @map("UNIT PRICE") @db.Decimal
  R_QTY              Decimal? @map("R-QTY") @db.Decimal
  AG_QTY             Decimal? @map("AG-QTY") @db.Decimal
  AMOUNT             Decimal  @db.Decimal
  COST               Decimal  @db.Decimal
  PROFIT_LOSS        Float?   @map("PROFIT/LOSS")
  P_L                Float?   @map("% P/L")

  @@id([DOC_ID, MAT_ID, AMOUNT, COST])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lableaderstock {
  id                  Int     @id @default(autoincrement())
  MATUnit             String?
  PO_                 String? @map("PO#")
  PO_Date             String? @map("PO Date")
  PO_REF              String? @map("PO REF")
  Purchaser_Personnel String? @map("Purchaser Personnel")
  PR_                 String? @map("PR#")
  Lot                 String?
  MAT_IN              String? @map("MAT IN")
  MAT_IN_Ref          String? @map("MAT IN Ref")
  INV_ID_MATIN_       String? @map("INV ID(MATIN)")
  Received_Date_Time  String? @map("Received Date Time")
  Location_ID         String? @map("Location ID")
  Unit_Cost           Float?  @map("Unit Cost")
  Cost_BLN            Float?  @map("Cost BLN")
  QTY_BLN_1_          Int?    @map("QTY BLN(1)")
  QTY_Ready_2_        Int?    @map("QTY Ready(2)")
  QTY_PN_Out_3_       Int?    @map("QTY PN Out(3)")
  QTY_1_2             Int?    @map("QTY 1+2")
  QTY_1_2_3           Int?    @map("QTY 1+2-3")
  QTY_1_3             Int?    @map("QTY 1-3")
  Unit                String?
  Age                 Int?
  Expired             String?
  Avail__Life         Int?    @map("Avail. Life")
  Product_Life        String? @map("Product Life")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model liststock {
  id          Int          @id @default(autoincrement())
  created_at  DateTime     @default(now()) @db.Timestamptz(6)
  product     Int?         @unique
  stock       Boolean?
  Productdata Productdata? @relation(fields: [product], references: [id], onDelete: Cascade)

  @@schema("public")
}

model matcat {
  id                        Int      @id @default(autoincrement())
  created_at                DateTime @default(now()) @db.Timestamptz(6)
  matcat                    String?
  brand                     Int?
  brand_matcat_brandTobrand brand?   @relation("matcat_brandTobrand", fields: [brand], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model messages {
  id           String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  chat_room_id String?       @db.Uuid
  sender_id    String?       @db.Uuid
  content      String?
  created_at   DateTime?     @default(now()) @db.Timestamp(6)
  type         String?
  status       String?
  chat_rooms   chat_rooms?   @relation(fields: [chat_room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users        public_users? @relation(fields: [sender_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model n8n_chat_histories {
  id         Int    @id @default(autoincrement())
  session_id String @db.VarChar(255)
  message    Json

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model noti_channel_stats {
  noti_id      Int          @id
  platform     String
  ok           Boolean?
  sent_count   Int?
  view_count   Int          @default(0)
  click_count  Int          @default(0)
  error_msg    String?
  last_sent_at DateTime?    @db.Timestamptz(6)
  updated_at   DateTime     @default(now()) @db.Timestamptz(6)
  customernoti customernoti @relation(fields: [noti_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([noti_id, platform])
  @@index([noti_id], map: "noti_channel_stats_noti_idx")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model noti_read_log {
  id           Int          @id @default(autoincrement())
  noti_id      Int
  user_id      String?      @db.Uuid
  platform     String       @default("app")
  event_type   String
  created_at   DateTime     @default(now()) @db.Timestamptz(6)
  customernoti customernoti @relation(fields: [noti_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users        auth_users?  @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([noti_id], map: "noti_read_log_noti_idx")
  @@schema("public")
}

model notification_logs {
  id                String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id           String       @db.Uuid
  point_id          String       @db.Uuid
  notification_type String
  sent_at           DateTime?    @default(now()) @db.Timestamptz(6)
  status            String?      @default("sent")
  metadata          Json?
  customeruser      customeruser @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([point_id, notification_type, user_id])
  @@index([sent_at], map: "idx_notification_logs_sent")
  @@index([user_id], map: "idx_notification_logs_user")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order {
  created_at          DateTime         @default(now()) @db.Timestamptz(6)
  member_id           String?          @db.Uuid
  invoicehead_id      Int?
  delivery            String?
  payment_type        Int?
  payment_date        DateTime?        @db.Timestamptz(6)
  parmanet_referrence String?
  id                  String           @id @unique @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  status              String           @default("pending")
  total_amount        Decimal?         @db.Decimal
  subtotal            Decimal?         @db.Decimal
  discount            Decimal?         @db.Decimal
  shipping_fee        Decimal?         @db.Decimal
  note                String?
  invoice             String?
  log                 String?
  voucher_id          Int?
  voucher_discount    Decimal?         @default(0) @db.Decimal
  customeruser        customeruser?    @relation(fields: [member_id], references: [authid], onDelete: SetDefault)
  payment_methods     payment_methods? @relation(fields: [payment_type], references: [id], onDelete: NoAction, onUpdate: NoAction)
  voucher             voucher?         @relation(fields: [voucher_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  order_product       order_product[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_product {
  id          Int          @id @default(autoincrement())
  created_at  DateTime     @default(now()) @db.Timestamptz(6)
  order_id    String?      @db.Uuid
  product_id  Int?
  quatity     Decimal?     @db.Decimal
  price       Decimal?     @db.Decimal
  total_price Decimal?     @db.Decimal
  order       order?       @relation(fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  Productdata Productdata? @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model page_permissions {
  page_key      String   @id
  page_label    String
  allowed_users String[] @default([]) @db.Uuid

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payment_methods {
  id                     Int                             @id @default(autoincrement())
  name                   String
  icon                   String                          @default("credit_card")
  icon_color             String                          @default("blue")
  is_active              Boolean                         @default(true)
  sort_order             Int                             @default(0)
  created_at             DateTime                        @default(now()) @db.Timestamptz(6)
  image_url              String?
  category               String?
  short_code             String?
  order                  order[]
  product_payment_method public_product_payment_method[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payment_type {
  id           Int      @id @default(autoincrement())
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  payment_type String?

  @@schema("public")
}

model po_goods {
  id             Int          @id @default(autoincrement())
  created_at     DateTime     @default(now()) @db.Timestamptz(6)
  docid          Int
  mat_cat        String?
  mat_name       String?
  qty            Int
  price          Decimal      @db.Decimal
  brand          String?
  int_matcat     String?
  minprice       Decimal?     @db.Decimal
  productdata_id Int?
  PO             PO           @relation(fields: [docid], references: [id], onDelete: Cascade)
  Productdata    Productdata? @relation(fields: [productdata_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model poprocess {
  id            Int      @id @default(autoincrement())
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  sharepoint_id Int?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model popup_image {
  id                         Int                @id @default(autoincrement())
  created_at                 DateTime           @default(now()) @db.Timestamptz(6)
  image_url                  String?
  active                     Boolean?
  start                      DateTime?          @db.Date
  end                        DateTime?          @db.Date
  tier                       String?
  promotion_id               Int?
  target_event_id            String?            @db.Uuid
  target_seminar_dwid        String?
  target_seminar_company     String?
  weblink                    String?
  navigation                 String?
  target_reading_campaign_id String?
  segment_id                 String?            @db.Uuid
  promotion                  public_promotion?  @relation(fields: [promotion_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customer_segment           customer_segment?  @relation(fields: [segment_id], references: [id], onUpdate: NoAction)
  events                     public_events?     @relation(fields: [target_event_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  reading_campaigns          reading_campaigns? @relation(fields: [target_reading_campaign_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model process_options {
  id            Int               @id @default(autoincrement())
  created_at    DateTime          @default(now()) @db.Timestamptz(6)
  name          String
  department_id Int?
  company_id    Int?
  companies     public_companies? @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  departments   departments?      @relation(fields: [department_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_product_categories {
  id         String            @id
  name       String?
  slug       String?
  parent_id  String?
  ancestors  String[]
  level      Int?
  sort_order Int?
  image_url  String?
  is_active  Boolean?
  created_at DateTime?         @db.Timestamptz(6)
  updated_at DateTime?         @db.Timestamptz(6)
  products   public_products[]

  @@map("product_categories")
  @@schema("public")
}

model public_product_images {
  id         Int              @id @default(autoincrement())
  product_id Int?
  url_image  String?
  is_show    Boolean?
  created_at DateTime?        @db.Timestamptz(6)
  updated_at DateTime?        @db.Timestamptz(6)
  products   public_products? @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("product_images")
  @@schema("public")
}

model product_likes {
  id         Int       @id @default(autoincrement())
  product_id Int
  user_id    String    @db.Uuid
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([product_id, user_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_manager {
  id                         Int           @id @default(autoincrement())
  created_at                 DateTime      @default(now()) @db.Timestamptz(6)
  mat_category               String?
  productmanager_user_authid String?       @db.Uuid
  users                      public_users? @relation(fields: [productmanager_user_authid], references: [authid], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_product_options {
  id                   Int              @id @default(autoincrement())
  product_id           Int?
  mat_identity         String?
  option_name          String?
  online_price         Float?
  min_price            Float?
  created_at           DateTime?        @db.Timestamptz(6)
  row_no               Int?
  updated_at           DateTime?        @db.Timestamptz(6)
  productdata_id       Int?
  is_stock             Boolean?
  attributes_hierarchy Int[]
  products             public_products? @relation(fields: [product_id], references: [id])
  Productdata          Productdata?     @relation(fields: [productdata_id], references: [id], onDelete: Cascade)

  @@map("product_options")
  @@schema("public")
}

model public_product_payment_method {
  id                Int              @id @default(autoincrement())
  product_id        Int?
  created_at        DateTime?        @db.Timestamptz(6)
  updated_at        DateTime?        @db.Timestamptz(6)
  payment_method_id Int?
  payment_methods   payment_methods? @relation(fields: [payment_method_id], references: [id], onDelete: Cascade)
  products          public_products? @relation(fields: [product_id], references: [id], onDelete: Cascade)

  @@map("product_payment_method")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_review {
  id                                     String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id                               String
  product_id                             Int
  user_id                                String      @db.Uuid
  rating                                 Int         @db.SmallInt
  comment                                String      @default("")
  created_at                             DateTime    @default(now()) @db.Timestamptz(6)
  status                                 String      @default("pending")
  admin_reply                            String?
  replied_at                             DateTime?   @db.Timestamptz(6)
  replied_by                             String?     @db.Uuid
  updated_at                             DateTime    @default(now()) @db.Timestamptz(6)
  users_product_review_replied_byTousers auth_users? @relation("product_review_replied_byTousers", fields: [replied_by], references: [id], onUpdate: NoAction)
  users_product_review_user_idTousers    auth_users  @relation("product_review_user_idTousers", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([order_id, product_id, user_id], map: "product_review_unique")
  @@index([created_at(sort: Desc)], map: "idx_product_review_created_at")
  @@index([rating], map: "idx_product_review_rating")
  @@index([status], map: "idx_product_review_status")
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model product_sale {
  id                   Int          @id(map: "Product_sale_pkey") @default(autoincrement())
  created_at           DateTime     @default(now()) @db.Timestamptz(6)
  cat                  String?      @unique(map: "Product_sale_cat_key")
  mat                  String?
  min_price            Decimal?     @db.Decimal
  productdata_id       Int?
  attributes_hierarchy Int?
  Productdata          Productdata? @relation(fields: [productdata_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "Product_sale_productdata_id_fkey")

  @@unique([cat, mat], map: "Product_sale_cat_mat_key")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_target {
  id           Int               @id(map: "produxt_target_pkey") @default(autoincrement())
  created_at   DateTime          @default(now()) @db.Timestamptz(6)
  user_id      String?           @db.Uuid
  target_date  DateTime?         @db.Timestamptz(6)
  companies_id Int?
  matcat       String?
  target       Decimal?          @db.Decimal
  status       Boolean?
  companies    public_companies? @relation(fields: [companies_id], references: [id], onDelete: Cascade, map: "produxt_target_companies_id_fkey")
  users        public_users?     @relation(fields: [user_id], references: [id], onDelete: Cascade, map: "produxt_target_user_id_fkey")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_products {
  id                     Int                             @id @default(autoincrement())
  product_name           String?
  product_description    String?
  company_id             Int?
  brand_id               Int?
  created_at             DateTime?                       @db.Timestamptz(6)
  updated_at             DateTime?                       @db.Timestamptz(6)
  is_active              Boolean?
  category_hierarchy     String[]
  unit                   String?
  is_online_active       Boolean?
  category_id            String?
  video_product          String?
  condition_description  String?
  warranty_description   String?
  youtube_url            String?
  is_pre_order           Boolean?
  is_custom_options      Boolean?
  product_images         public_product_images[]
  product_options        public_product_options[]
  product_payment_method public_product_payment_method[]
  brands                 public_brands?                  @relation(fields: [brand_id], references: [id], onDelete: Cascade)
  product_categories     public_product_categories?      @relation(fields: [category_id], references: [id], onDelete: Cascade)
  companies              public_companies?               @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@map("products")
  @@schema("public")
}

model producttype {
  id                                                             Int                  @id @default(autoincrement())
  created_at                                                     DateTime             @default(now()) @db.Timestamptz(6)
  type                                                           String?
  active                                                         Boolean?
  Productdata                                                    Productdata[]
  producttype_level2_producttype_level2_producttypeToproducttype producttype_level2[] @relation("producttype_level2_producttypeToproducttype")

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model producttype_level2 {
  id                                                                           Int                  @id @default(autoincrement())
  created_at                                                                   DateTime             @default(now()) @db.Timestamptz(6)
  type                                                                         String?
  active                                                                       Boolean?
  producttype                                                                  Int?
  Productdata                                                                  Productdata[]
  producttype_producttype_level2_producttypeToproducttype                      producttype?         @relation("producttype_level2_producttypeToproducttype", fields: [producttype], references: [id], onDelete: NoAction, onUpdate: NoAction)
  producttype_level3_producttype_level3_producttype_level2Toproducttype_level2 producttype_level3[] @relation("producttype_level3_producttype_level2Toproducttype_level2")

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model producttype_level3 {
  id                                                                           Int                 @id @default(autoincrement())
  created_at                                                                   DateTime            @default(now()) @db.Timestamptz(6)
  type                                                                         String?
  active                                                                       Boolean?
  producttype_level2                                                           Int?
  Productdata                                                                  Productdata[]
  producttype_level2_producttype_level3_producttype_level2Toproducttype_level2 producttype_level2? @relation("producttype_level3_producttype_level2Toproducttype_level2", fields: [producttype_level2], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_promotion {
  id                                      Int                                         @id @default(autoincrement())
  created_at                              DateTime                                    @default(now()) @db.Timestamptz(6)
  promotion_name                          String?                                     @map("proname")
  promotion_start                         DateTime?                                   @map("startdate") @db.Timestamptz(6)
  promotion_end                           DateTime?                                   @map("enddate") @db.Timestamptz(6)
  filelink                                String?
  banner                                  String?
  promotion_description                   String?                                     @map("description")
  promotion_type                          String?                                     @map("type")
  points_reward                           Float?                                      @default(0)
  max_claims                              Int?
  current_claims                          Int?                                        @default(0)
  voucher_id                              Int?
  location_lat                            Float?
  location_lng                            Float?
  location_radius_m                       Int?                                        @default(200)
  location_name                           String?
  conditions                              String?
  per_person_limit                        Int?
  subtype                                 String?
  is_active                               Boolean?                                    @map("status")
  sort_order                              Int?
  url_image                               String?
  promotion_image                         String?
  updated_at                              DateTime?                                   @db.Timestamptz(6)
  is_accept_overlapse_promotion           Boolean?
  customer_tiers                          String[]
  popup_image                             popup_image[]
  voucher                                 voucher?                                    @relation(fields: [voucher_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  promotion_analytics                     promotion_analytics[]
  promotion_bundle_deal_get_products      public_promotion_bundle_deal_get_products[]
  promotion_bundle_deal_grand_total_tiers promotion_bundle_deal_grand_total_tiers[]
  promotion_claim_history                 promotion_claim_history[]
  promotion_discount_products             public_promotion_discount_products[]
  promotion_extra_points_products         public_promotion_extra_points_products[]
  promotion_extra_points_tier_rules       promotion_extra_points_tier_rules[]
  promotion_flashsale_products            public_promotion_flashsale_products[]
  promotion_gift                          promotion_gift[]
  promotion_product                       promotion_product[]
  promotion_redeem                        promotion_redeem[]
  promotion_voucher                       promotion_voucher[]

  @@map("promotion")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model promotion_analytics {
  id           String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  promotion_id Int
  member_id    String?
  event        String
  created_at   DateTime?        @default(now()) @db.Timestamptz(6)
  promotion    public_promotion @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([member_id])
  @@index([promotion_id])
  @@schema("public")
}

model public_promotion_bundle_deal_free_products {
  id                                 Int                                        @id @default(autoincrement())
  bundle_deal_get_id                 Int?
  product_option_id                  Int?
  free_quantity                      Int?
  promotion_bundle_deal_get_products public_promotion_bundle_deal_get_products? @relation(fields: [bundle_deal_get_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_bundle_deal_free_products_bundle_get_id_fkey")

  @@map("promotion_bundle_deal_free_products")
  @@schema("public")
}

model public_promotion_bundle_deal_get_products {
  id                                  Int                                          @id(map: "promotion_bundle_deal_products_pkey") @default(autoincrement())
  promotion_id                        Int?
  product_option_id                   Int?
  get_quantity                        Int?
  promotion_bundle_deal_free_products public_promotion_bundle_deal_free_products[]
  promotion                           public_promotion?                            @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_bundle_deal_products_promotion_id_fkey")

  @@map("promotion_bundle_deal_get_products")
  @@schema("public")
}

model public_promotion_bundle_deal_grand_total_free_products {
  id                                      Int                                      @id(map: "promotion_bundle_deal_grand_total_free_pkey") @default(autoincrement())
  bundle_deal_grand_total_tiers_id        Int?
  product_option_id                       Int?
  free_quantity                           Int?
  promotion_bundle_deal_grand_total_tiers promotion_bundle_deal_grand_total_tiers? @relation(fields: [bundle_deal_grand_total_tiers_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_bundle_deal_grand_t_bundle_deal_grand_total_tier_fkey")

  @@map("promotion_bundle_deal_grand_total_free_products")
  @@schema("public")
}

model promotion_claim_history {
  id             Int              @id @default(autoincrement())
  promotion_id   Int
  user_id        String           @db.Uuid
  claimed_at     DateTime?        @default(now()) @db.Timestamp(6)
  points_awarded Int
  voucher_id     Int?
  promotion      public_promotion @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  customeruser   customeruser     @relation(fields: [user_id], references: [id], onDelete: Cascade)
  voucher        voucher?         @relation(fields: [voucher_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([promotion_id, user_id])
  @@index([promotion_id], map: "idx_promotion_claim_promo")
  @@index([user_id, promotion_id], map: "idx_promotion_claim_user")
  @@schema("public")
}

model public_promotion_discount_products {
  id                Int               @id(map: "discount_products_pkey") @default(autoincrement())
  promotion_id      Int?
  product_option_id Int?
  sale_price        Float?
  sale_percent      Float?
  promotion         public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("promotion_discount_products")
  @@schema("public")
}

model public_promotion_extra_points_products {
  id                Int               @id(map: "promotion_extra_points_product_point_pkey") @default(autoincrement())
  promotion_id      Int?
  product_option_id Int?
  points_multiplier Float?
  promotion         public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "promotion_extra_points_product_point_promotion_id_fkey")

  @@map("promotion_extra_points_products")
  @@schema("public")
}

model public_promotion_flashsale_products {
  id                Int               @id(map: "flashsale_products_pkey") @default(autoincrement())
  promotion_id      Int?
  product_option_id Int?
  sale_price        Float?
  sale_percent      Float?
  promotion         public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@map("promotion_flashsale_products")
  @@schema("public")
}

model promotion_gift {
  id                                                         Int              @id @default(autoincrement())
  created_at                                                 DateTime?        @default(now()) @db.Timestamptz(6)
  promotion_id                                               Int
  trigger_product_id                                         Int?
  min_quantity                                               Int              @default(1)
  min_amount                                                 Decimal?         @db.Decimal
  gift_product_id                                            Int
  gift_quantity                                              Int              @default(1)
  Productdata_promotion_gift_gift_product_idToProductdata    Productdata      @relation("promotion_gift_gift_product_idToProductdata", fields: [gift_product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  promotion                                                  public_promotion @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  Productdata_promotion_gift_trigger_product_idToProductdata Productdata?     @relation("promotion_gift_trigger_product_idToProductdata", fields: [trigger_product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model promotion_product {
  id              Int               @id @default(autoincrement())
  created_at      DateTime          @default(now()) @db.Timestamptz(6)
  promotion_id    Int?
  product_id      Int?
  promotion_price Decimal?          @db.Decimal
  qty             Decimal?          @db.Decimal
  Productdata     Productdata?      @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  promotion       public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model promotion_redeem {
  id               Int               @id @default(autoincrement())
  created_at       DateTime          @default(now()) @db.Timestamptz(6)
  promotion_id     Int?
  promotion_price  Decimal?          @db.Decimal
  qty              Decimal?          @db.Decimal
  rewards_id       String            @db.Uuid
  per_person_limit Int?              @db.SmallInt
  promotion        public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  rewards          rewards           @relation(fields: [rewards_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model promotion_voucher {
  id           Int               @id @default(autoincrement())
  promotion_id Int?
  voucher_id   Int?
  created_at   DateTime?         @default(now()) @db.Timestamptz(6)
  promotion    public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  voucher      voucher?          @relation(fields: [voucher_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reading_campaigns {
  id                  String          @id
  title               String
  description         String?
  required_reads      Int             @default(5)
  points_reward       Int
  start_date          DateTime?       @db.Timestamptz(6)
  end_date            DateTime?       @db.Timestamptz(6)
  max_redemptions     Int?
  current_redemptions Int?            @default(0)
  active              Boolean?        @default(true)
  created_at          DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?       @default(now()) @db.Timestamptz(6)
  banner              String?
  pdf                 String?
  reset_after_minutes Int?            @default(10080)
  content_types       String[]        @default([])
  popup_image         popup_image[]
  reward_points       reward_points[]

  @@index([active], map: "idx_campaigns_active")
  @@index([content_types], map: "idx_campaigns_content_types", type: Gin)
  @@index([start_date, end_date], map: "idx_campaigns_dates")
  @@index([active], map: "idx_reading_campaigns_active", where: raw("(active = true)"))
  @@index([start_date, end_date], map: "idx_reading_campaigns_dates")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model record_manager {
  id                    Int                     @id @default(autoincrement())
  created_at            DateTime                @default(now()) @db.Timestamptz(6)
  file_id               String?
  hash                  String?
  data_type             String?
  schema                String?
  document_title        String?
  company               String?
  tabular_document_rows tabular_document_rows[]

  @@schema("public")
}

model redemption_points {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  redemption_id      String             @db.Uuid
  reward_point_id    String             @db.Uuid
  points_consumed    Int                @default(0)
  reward_redemptions reward_redemptions @relation(fields: [redemption_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  reward_points      reward_points      @relation(fields: [reward_point_id], references: [id], onUpdate: NoAction)

  @@unique([redemption_id, reward_point_id])
  @@index([redemption_id], map: "idx_redemption_points_redemption_id")
  @@index([reward_point_id], map: "idx_redemption_points_reward_point_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model referral_fingerprints {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ref_code   String
  ip_address String
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  expires_at DateTime? @default(dbgenerated("(now() + '24:00:00'::interval)")) @db.Timestamptz(6)
  is_claimed Boolean?  @default(false)

  @@schema("public")
}

model referral_settings {
  id                          Int                           @id @default(autoincrement())
  points_reward               Int                           @default(100)
  is_active                   Boolean                       @default(true)
  name                        String?
  start_date                  DateTime?                     @db.Date
  end_date                    DateTime?                     @db.Date
  banner                      String?
  newuser_points_reward       Int?
  product_milestones          Json?                         @default("[]")
  milestone_mode              String                        @default("all")
  condition                   String?                       @default("")
  customeruser                customeruser[]
  referral_milestones_claimed referral_milestones_claimed[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reward_category {
  id                                               Int       @id(map: "reword_category_pkey") @default(autoincrement())
  created_at                                       DateTime  @default(now()) @db.Timestamptz(6)
  category                                         String?
  active                                           Boolean?
  icon                                             String?
  rewards_rewards_reward_categoryToreward_category rewards[] @relation("rewards_reward_categoryToreward_category")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reward_points {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id           String?             @db.Uuid
  points            Int
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  expired_at        DateTime            @default(dbgenerated("(now() + '1 year'::interval)")) @db.Timestamptz(6)
  doc_id            String?
  redeem_point      Decimal?            @db.Decimal
  redeem_exp        DateTime?           @default(dbgenerated("(now() + '3 years'::interval)")) @db.Date
  reason            String?
  log               String?
  doc_date          String?
  campaign_id       String?
  redemption_points redemption_points[]
  reading_campaigns reading_campaigns?  @relation(fields: [campaign_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser      customeruser?       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([campaign_id], map: "idx_reward_point_campaign")
  @@index([user_id, campaign_id], map: "idx_reward_point_user_campaign")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reward_redemptions {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id           String              @db.Uuid
  reward_id         String              @db.Uuid
  points_used       Int
  redeemed_at       DateTime?           @default(now()) @db.Timestamptz(6)
  status            String?             @default("pending")
  note              String?
  redeemed_count    Decimal?            @db.Decimal
  address           String?
  address_type      String?
  delivery_status   String?
  updated_at        String?
  log               String?
  email_resend_logs email_resend_logs[]
  redemption_points redemption_points[]
  rewards           rewards             @relation(fields: [reward_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  customeruser      customeruser        @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "reward_redemptions_user_id_fkey1")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rewards {
  id                                                       String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                                                     String
  description                                              String?
  points_required                                          Int
  stock                                                    Int?                 @default(0)
  active                                                   Boolean?             @default(true)
  reward_category                                          Int
  pic                                                      String?
  condition                                                String?
  promotion_redeem                                         promotion_redeem[]
  reward_redemptions                                       reward_redemptions[]
  reward_category_rewards_reward_categoryToreward_category reward_category      @relation("rewards_reward_categoryToreward_category", fields: [reward_category], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model rich_menu_config {
  id                     Int                    @id @default(autoincrement())
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  role                   String?
  rich_menu_id           String?
  is_active              Boolean?
  description            String?
  start_date             DateTime?              @db.Timestamptz(6)
  end_date               DateTime?              @db.Timestamptz(6)
  priority               Int?
  channel_config_id      String                 @db.Uuid
  social_channel_configs social_channel_configs @relation(fields: [channel_config_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([channel_config_id], map: "rich_menu_config_channel_idx")
  @@schema("public")
}

model rpa_queue {
  id            Int       @id @default(autoincrement())
  job_type      String
  payload       Json?
  status        String?   @default("pending")
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  started_at    DateTime? @db.Timestamptz(6)
  finished_at   DateTime? @db.Timestamptz(6)
  error_message String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sale_target {
  id           Int               @id @default(autoincrement())
  created_at   DateTime          @default(now()) @db.Timestamptz(6)
  user_id      String?           @db.Uuid
  target_date  DateTime?         @db.Timestamptz(6)
  companies_id Int?
  matcat       String?
  target       Decimal?          @db.Decimal
  status       Boolean?
  companies    public_companies? @relation(fields: [companies_id], references: [id], onDelete: Cascade)
  users        public_users?     @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sale_target_attr {
  id           Int                @id @default(autoincrement())
  created_at   DateTime?          @default(now()) @db.Timestamptz(6)
  user_id      String?            @db.Uuid
  target_date  DateTime?          @db.Timestamptz(6)
  attr_id      Int?
  target       Decimal?           @db.Decimal
  status       Boolean?           @default(false)
  companies_id Int?
  attributes   public_attributes? @relation(fields: [attr_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users        public_users?      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

model sales_leads {
  id               Int       @id @default(autoincrement())
  conversation_id  String?
  contact_name     String?
  platform         String?
  product_interest String?
  status           String    @default("new")
  note             String?
  sale_amount      Decimal?  @db.Decimal
  staff_name       String?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model scheduled_deletions {
  id              Int      @id @default(autoincrement())
  user_profile_id String
  scheduled_at    DateTime @db.Timestamptz(6)
  status          String   @default("pending")
  created_at      DateTime @default(now()) @db.Timestamptz(6)

  @@index([scheduled_at], map: "idx_scheduled_at")
  @@index([status], map: "idx_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model search_stats {
  id         Int       @id @default(autoincrement())
  keyword    String    @unique
  count      Int?      @default(1)
  updated_at DateTime? @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  type       String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model shipping_api_settings {
  id         Int      @id @default(autoincrement())
  company_id Int
  provider   String
  label      String?
  config     Json     @default("{}")
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@unique([company_id, provider])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_channel_configs {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  platform               String
  name                   String
  access_token           String?
  channel_secret         String?
  page_id                String?
  verify_token           String?
  webhook_verified       Boolean?                 @default(false)
  active                 Boolean?                 @default(true)
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  app_id                 String?
  refresh_token          String?
  token_expires_at       DateTime?                @db.Timestamptz(6)
  oauth_type             String?
  customer_line_accounts customer_line_accounts[]
  rich_menu_config       rich_menu_config[]
  social_conversations   social_conversations[]
  staff_line_accounts    staff_line_accounts[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_conversations {
  id                     String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  channel_config_id      String?                 @db.Uuid
  platform               String
  external_id            String
  contact_name           String?
  contact_avatar         String?
  last_message           String?
  last_message_at        DateTime?               @db.Timestamptz(6)
  unread_count           Int?                    @default(0)
  created_at             DateTime?               @default(now()) @db.Timestamptz(6)
  tags                   String[]                @default([])
  note                   String?                 @default("")
  crm_status             String?                 @default("")
  social_channel_configs social_channel_configs? @relation(fields: [channel_config_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  social_messages        social_messages[]

  @@unique([channel_config_id, external_id])
  @@index([channel_config_id], map: "idx_social_conversations_channel")
  @@index([platform], map: "idx_social_conversations_platform")
  @@index([last_message_at(sort: Desc)], map: "idx_social_conversations_updated")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model social_messages {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  conversation_id      String?               @db.Uuid
  direction            String
  content              String?
  message_type         String?               @default("text")
  media_url            String?
  external_msg_id      String?
  sender_name          String?
  created_at           DateTime?             @default(now()) @db.Timestamptz(6)
  read_at              DateTime?             @db.Timestamptz(6)
  reply_token          String?
  source_type          String?               @default("message")
  comment_id           String?
  post_id              String?
  post_snippet         String?
  social_conversations social_conversations? @relation(fields: [conversation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([conversation_id], map: "idx_social_messages_conversation")
  @@index([created_at], map: "idx_social_messages_created")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sopt_goods {
  id           Int       @id @default(autoincrement())
  sopt_head_id Int
  product_code String?
  product_name String?
  qty          Int?      @default(1)
  price        Decimal?  @default(0) @db.Decimal(15, 2)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  sopt_head    sopt_head @relation(fields: [sopt_head_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sopt_head {
  id                Int                 @id @default(autoincrement())
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  customeruser_id   String?             @db.Uuid
  contact_id        Int?
  doc_id            String?
  users_id          String?             @db.Uuid
  budget_year       String?
  end_date          DateTime?           @db.Timestamptz(6)
  IM                IM[]
  customer_requests customer_requests[]
  sopt_goods        sopt_goods[]
  contact           contact?            @relation(fields: [contact_id], references: [id], onDelete: Cascade)
  customeruser      customeruser?       @relation(fields: [customeruser_id], references: [id], onDelete: Cascade)
  users             public_users?       @relation(fields: [users_id], references: [id], onDelete: Cascade)
  sopt_status       sopt_status[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sopt_status {
  id               Int               @id @default(autoincrement())
  created_at       DateTime          @default(now()) @db.Timestamptz(6)
  status_id        Int?
  update_by        String?           @db.Uuid
  detail           String?
  sopt_head_id     Int?
  note             String?
  sopt_head        sopt_head?        @relation(fields: [sopt_head_id], references: [id], onDelete: Cascade)
  sopt_status_data sopt_status_data? @relation(fields: [status_id], references: [id], onDelete: Cascade)
  users            public_users?     @relation(fields: [update_by], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sopt_status_data {
  id          Int           @id @default(autoincrement())
  created_at  DateTime      @default(now()) @db.Timestamptz(6)
  status_name String?
  deivsion_id Int?
  percent     Int?
  is_process  Boolean?
  is_active   Boolean?
  sort_order  Int?
  stage_name  String?
  sopt_status sopt_status[]
  departments departments?  @relation(fields: [deivsion_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model survey_answers {
  id               Int               @id @default(autoincrement())
  response_id      Int?
  question_id      Int?
  answer_text      String?
  answer_value     Json?
  created_at       DateTime?         @default(now()) @db.Timestamptz(6)
  survey_questions survey_questions? @relation(fields: [question_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  survey_responses survey_responses? @relation(fields: [response_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model survey_questions {
  id             Int              @id @default(autoincrement())
  survey_id      Int?
  question_text  String
  question_type  String
  options        Json?
  is_required    Boolean?         @default(true)
  order_index    Int?             @default(0)
  created_at     DateTime?        @default(now()) @db.Timestamptz(6)
  survey_answers survey_answers[]
  surveys        surveys?         @relation(fields: [survey_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([survey_id], map: "idx_survey_questions_survey")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model survey_responses {
  id             Int              @id @default(autoincrement())
  survey_id      Int?
  customer_id    String?          @db.Uuid
  submitted_at   DateTime?        @default(now()) @db.Timestamptz(6)
  points_awarded Int?             @default(0)
  survey_answers survey_answers[]
  customeruser   customeruser?    @relation(fields: [customer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  surveys        surveys?         @relation(fields: [survey_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([survey_id, customer_id])
  @@index([customer_id], map: "idx_survey_responses_customer")
  @@index([survey_id], map: "idx_survey_responses_survey")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model surveys {
  id                Int                @id @default(autoincrement())
  title             String
  description       String?
  banner            String?
  points_reward     Int                @default(0)
  start_date        DateTime           @db.Date
  end_date          DateTime           @db.Date
  allowed_tiers     String[]           @default([])
  max_responses     Int?
  current_responses Int?               @default(0)
  is_active         Boolean?           @default(true)
  created_at        DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?          @default(now()) @db.Timestamptz(6)
  survey_questions  survey_questions[]
  survey_responses  survey_responses[]

  @@index([start_date, end_date], map: "idx_surveys_dates")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tabular_document_rows {
  id                Int             @id(map: "tubular_document_rows_pkey") @default(autoincrement())
  created_at        DateTime        @default(now()) @db.Timestamptz(6)
  record_manager_id Int?
  row_data          Json?
  record_manager    record_manager? @relation(fields: [record_manager_id], references: [id], onDelete: Cascade, map: "tubular_document_rows_record_manager_id_fkey")

  @@schema("public")
}

model teams {
  id            Int            @id @default(autoincrement())
  name          String
  department_id Int?
  description   String?
  active        Boolean        @default(true)
  created_at    DateTime       @default(now()) @db.Timestamptz(6)
  departments   departments?   @relation(fields: [department_id], references: [id], onUpdate: NoAction)
  users         public_users[]

  @@index([department_id], map: "idx_teams_department")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_activity_log {
  id              Int       @id @default(autoincrement())
  auth_id         String?   @db.Uuid
  action          String?
  target_type     String?
  target_id       String?
  target_title    String?
  logged_at       DateTime? @db.Timestamptz(6)
  referral_source String?

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_favorites {
  id         Int        @id @default(autoincrement())
  user_id    String     @db.Uuid
  product_id Int
  created_at DateTime?  @default(now()) @db.Timestamptz(6)
  users      auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, product_id])
  @@schema("public")
}

model user_login_history {
  id           String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at   DateTime    @default(now()) @db.Timestamptz(6)
  auth_id      String?     @default(dbgenerated("auth.uid()")) @db.Uuid
  login_at     DateTime?   @db.Timestamptz(6)
  app_version  String?
  build_number String?
  platform     String?
  users        auth_users? @relation(fields: [auth_id], references: [id], onDelete: Cascade)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_page_permissions {
  id         Int      @id @default(autoincrement())
  user_id    String
  route      String
  can_view   Boolean  @default(true)
  can_add    Boolean  @default(true)
  can_edit   Boolean  @default(true)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@unique([user_id, route])
  @@index([user_id], map: "idx_user_page_perm_user")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_reading_progress {
  id            String       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id       String       @db.Uuid
  content_type  String
  content_id    String
  content_title String?
  read_at       DateTime?    @default(now()) @db.Timestamptz(6)
  campaign_id   String?      @default("read_5_campaign")
  create_at     DateTime?    @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at    DateTime?    @db.Timestamptz(6)
  last_read_at  DateTime?    @default(now()) @db.Timestamptz(6)
  customeruser  customeruser @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, content_type, content_id], map: "user_reading_progress_unique")
  @@unique([user_id, content_type, content_id, campaign_id])
  @@index([user_id, campaign_id], map: "idx_user_reading")
  @@index([user_id, campaign_id, last_read_at], map: "idx_user_reading_last_read")
  @@index([read_at(sort: Desc)], map: "idx_user_reading_progress_read_at")
  @@index([user_id], map: "idx_user_reading_progress_user")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_users {
  id                                      String                @id @unique @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                                    String                @unique
  avatar_url                              String?
  is_online                               Boolean?              @default(false)
  sharepoint_id                           Int?
  person_id                               String?
  email                                   String?
  line_id                                 String?
  planetone_id                            String?
  tel                                     String?
  position                                String?
  manager                                 String?               @db.Uuid
  manager2                                String?               @db.Uuid
  address                                 String?
  authid                                  String?               @unique @db.Uuid
  last_online                             String?
  fcm_token                               String?
  salesupport                             String?               @db.Uuid
  role                                    String?               @default("user")
  pdpa_accepted                           Boolean?
  pdpa_accepted_at                        DateTime?             @db.Timestamptz(6)
  pdpa_version                            String?
  department                              Int?
  company                                 Int?
  team                                    Int?
  is_active                               Boolean?
  extra_pages                             String[]              @default([])
  lineid_3nholdingapp                     String?
  IM                                      IM[]
  IO                                      IO[]
  PO_PO_create_byTousers                  PO[]                  @relation("PO_create_byTousers")
  PO_PO_update_byTousers                  PO[]                  @relation("PO_update_byTousers")
  Productdata                             Productdata[]
  addgoods                                addgoods[]
  asset_check                             asset_check[]
  assett_assett_installerTousers          assett[]              @relation("assett_installerTousers")
  assett_assett_recorded_byTousers        assett[]              @relation("assett_recorded_byTousers")
  assett_assett_sale_person_idTousers     assett[]              @relation("assett_sale_person_idTousers")
  assett_waranty                          assett_waranty[]
  ces_status                              ces_status[]
  chat_participants                       chat_participants[]
  contact                                 contact[]
  customer_requests                       customer_requests[]
  customeruser_users                      customeruser_users[]
  hidden_chats                            hidden_chats[]
  im_goods_im_goods_approved_by_pmTousers im_goods[]            @relation("im_goods_approved_by_pmTousers")
  im_goods_im_goods_users_idTousers       im_goods[]            @relation("im_goods_users_idTousers")
  io_goods                                io_goods[]
  messages                                messages[]
  product_manager                         product_manager[]
  product_target                          product_target[]
  sale_target                             sale_target[]
  sale_target_attr                        sale_target_attr[]
  sopt_head                               sopt_head[]
  sopt_status                             sopt_status[]
  staff_line_accounts                     staff_line_accounts[]
  users                                   auth_users?           @relation(fields: [authid], references: [id])
  companies                               public_companies?     @relation(fields: [company], references: [id], onDelete: NoAction, onUpdate: NoAction)
  departments                             departments?          @relation(fields: [department], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users_users_manager2Tousers             public_users?         @relation("users_manager2Tousers", fields: [manager2], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_users_users_manager2Tousers       public_users[]        @relation("users_manager2Tousers")
  users_users_managerTousers              public_users?         @relation("users_managerTousers", fields: [manager], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_users_users_managerTousers        public_users[]        @relation("users_managerTousers")
  users_users_salesupportTousers          public_users?         @relation("users_salesupportTousers", fields: [salesupport], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_users_users_salesupportTousers    public_users[]        @relation("users_salesupportTousers")
  teams                                   teams?                @relation(fields: [team], references: [id], onUpdate: NoAction)

  @@index([is_active], map: "idx_users_is_active")
  @@index([team], map: "idx_users_team")
  @@map("users")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model voucher {
  id                      Int                       @id @default(autoincrement())
  created_at              DateTime                  @default(now()) @db.Timestamptz(6)
  name                    String?
  detail                  String?
  pic                     String?
  condition               String?
  tier                    String?
  voucherpic              String?
  type                    String                    @default("amount")
  discount_value          Decimal                   @default(0) @db.Decimal
  max_discount            Decimal?                  @db.Decimal
  min_order               Decimal?                  @default(0) @db.Decimal
  combinable              Boolean                   @default(false)
  status                  Boolean                   @default(true)
  startdate               DateTime?                 @db.Timestamptz(6)
  enddate                 DateTime?                 @db.Timestamptz(6)
  discount_type           String?
  usage_limit             String?
  redemption_method       String?
  customernoti            customernoti[]
  customervoucher         customervoucher[]
  order                   order[]
  promotion               public_promotion[]
  promotion_claim_history promotion_claim_history[]
  promotion_voucher       promotion_voucher[]

  @@schema("public")
}

model voucher_email_log {
  id                 Int       @id @default(autoincrement())
  created_at         DateTime? @default(dbgenerated("timezone('utc'::text, now())")) @db.Timestamptz(6)
  action             String
  email              String
  voucher_name       String
  customervoucher_id Int?

  @@schema("public")
}

model webhook_retry_queue {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  table_name    String?
  payload       Json?
  retry_count   Int?      @default(0)
  next_retry_at DateTime? @default(now()) @db.Timestamptz(6)
  status        String?   @default("pending")

  @@index([next_retry_at], map: "idx_webhook_next_retry", where: raw("(status = 'pending'::text)"))
  @@schema("public")
}

model customer_tags {
  id           String       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  customer_id  String       @db.Uuid
  tag_id       String       @db.Uuid
  assigned_at  DateTime     @default(now()) @db.Timestamptz(6)
  expires_at   DateTime?    @db.Timestamptz(6)
  source       String       @default("manual")
  customeruser customeruser @relation(fields: [customer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tags         tags         @relation(fields: [tag_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([customer_id, tag_id])
  @@index([customer_id], map: "idx_customer_tags_customer")
  @@index([expires_at], map: "idx_customer_tags_expires", where: raw("(expires_at IS NOT NULL)"))
  @@index([tag_id], map: "idx_customer_tags_tag")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tags {
  id                                            String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                                          String
  color                                         String          @default("#6366f1")
  tag_type                                      String          @default("permanent")
  expire_days                                   Int?
  is_active                                     Boolean         @default(true)
  created_at                                    DateTime        @default(now()) @db.Timestamptz(6)
  customer_tags                                 customer_tags[]
  customernoti_customernoti_click_tag_idTotags  customernoti[]  @relation("customernoti_click_tag_idTotags")
  customernoti_customernoti_target_tag_idTotags customernoti[]  @relation("customernoti_target_tag_idTotags")

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model promotion_bundle_deal_grand_total_tiers {
  id                                              Int                                                      @id @default(autoincrement())
  promotion_id                                    Int?
  level_no                                        Int?
  minimum_grand_total                             Float?
  promotion_bundle_deal_grand_total_free_products public_promotion_bundle_deal_grand_total_free_products[]
  promotion                                       public_promotion?                                        @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model promotion_extra_points_tier_rules {
  id                Int               @id @default(autoincrement())
  promotion_id      Int?
  level_no          Int?
  min_amount        Float?
  points_multiplier Float?
  promotion         public_promotion? @relation(fields: [promotion_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model referral_milestones_claimed {
  id                 BigInt            @id @default(autoincrement())
  referrer_member_id String
  campaign_id        Int
  threshold          Int
  reward_id          String?
  claimed_at         DateTime          @default(now()) @db.Timestamptz(6)
  referral_settings  referral_settings @relation(fields: [campaign_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([referrer_member_id, campaign_id, threshold])
  @@index([referrer_member_id, campaign_id], map: "idx_rmc_referrer")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model bill_tracking_history {
  id                 Int      @id @default(autoincrement())
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  doc_no             String?
  line_id            String?
  line_office_accont String?

  @@unique([doc_no, line_id], map: "bill_tracking_doc_line_unique")
  @@schema("public")
}

model noti_auto_send_log {
  id           Int          @id @default(autoincrement())
  noti_id      Int
  customer_id  String       @db.Uuid
  sent_at      DateTime     @default(now()) @db.Timestamptz(6)
  customeruser customeruser @relation(fields: [customer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  customernoti customernoti @relation(fields: [noti_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([noti_id, customer_id])
  @@index([noti_id], map: "idx_noti_auto_send_log_noti")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_invoice_address {
  id              Int          @id @default(autoincrement())
  customeruser_id String?      @db.Uuid
  company_name    String?
  tax_no          String?
  entity_id       String?
  entity_name     String?
  branch_name     String?
  branch_code     String?
  address_line1   String?
  address_line2   String?
  sub_district    String?
  district        String?
  province        String?
  post_code       String?
  set_default     Boolean?
  invoicehead_id  Int?
  invoicehead     invoicehead? @relation(fields: [invoicehead_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("3nconnect")
}

model aecsales {
  ID          Int     @id @default(autoincrement())
  DATE        String?
  DOC_ID      String? @map("DOC ID")
  C__REF      String? @map("C. REF")
  CUSTOMER    String?
  C__BUYER    String? @map("C. BUYER")
  C__PAYMENT  String? @map("C. PAYMENT")
  SO_TYPE     String? @map("SO TYPE")
  MAT_CAT     String? @map("MAT CAT")
  MAT_ID      String? @map("MAT ID")
  MAT         String?
  UNIT        String?
  PERSONNEL   String?
  UNIT_PRICE  Float?  @map("UNIT PRICE")
  R_QTY       Int?    @map("R-QTY")
  AG_QTY      Int?    @map("AG-QTY")
  AMOUNT      Float?
  COST        Float?
  PROFIT_LOSS Float?  @map("PROFIT/LOSS")
  P_L         Float?  @map("% P/L")

  @@schema("public")
}

model assett_goods_docs {
  id              Int          @id @default(autoincrement())
  created_at      DateTime     @default(now()) @db.Timestamptz(6)
  assett_goods_id Int
  assett_id       Int
  assett          assett       @relation(fields: [assett_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "assett_goods_docs_assett_fkey")
  assett_goods    assett_goods @relation(fields: [assett_goods_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "assett_goods_docs_goods_fkey")

  @@unique([assett_goods_id, assett_id], map: "assett_goods_docs_unique")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model report_inventory_snapshot {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_short String
  period        DateTime @db.Date
  cost_bln      Decimal  @db.Decimal
  created_at    DateTime @default(now()) @db.Timestamptz(6)

  @@unique([company_short, period])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model report_recipients {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email      String   @unique
  name       String?
  active     Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_line_accounts {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customeruser_id        String                 @db.Uuid
  channel_config_id      String                 @db.Uuid
  line_user_id           String
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  social_channel_configs social_channel_configs @relation(fields: [channel_config_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  customeruser           customeruser           @relation(fields: [customeruser_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([channel_config_id, line_user_id])
  @@unique([customeruser_id, channel_config_id])
  @@index([customeruser_id])
  @@index([line_user_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model staff_line_accounts {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                String                 @db.Uuid
  channel_config_id      String                 @db.Uuid
  line_user_id           String
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  social_channel_configs social_channel_configs @relation(fields: [channel_config_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  public_users           @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([channel_config_id, line_user_id])
  @@unique([user_id, channel_config_id])
  @@index([line_user_id])
  @@index([user_id])
  @@schema("public")
}

view vw_all_events {
  event_id         String?  @db.Uuid
  event_image      String?
  event_cate_name  String?
  event_name       String?
  event_detail     String?
  register_date    String?
  preregister_date String?
  event_startdate  String?
  event_enddate    String?
  location_name    String?
  tier_register    String?
  tier_preregister String?
  ref_url          String?
  is_active        Boolean?

  @@schema("3nconnect")
}

view vw_bundle_deal_get_x_free_y {
  promotion_type        String?
  promotion_name        String?
  get_company_name      String?
  get_brand_name        String?
  get_product_category  String?
  get_mat_identity      String?
  get_product_name      String?
  get_product_unit      String?
  get_quantity          Int?
  free_company_name     String?
  free_brand_name       String?
  free_product_category String?
  free_mat_identity     String?
  free_product_name     String?
  free_product_unit     String?
  free_quantity         Int?
  promotion_start       DateTime? @db.Timestamptz(6)
  promotion_end         DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

view vw_customer_information {
  user_id         String?   @db.Uuid
  auth_id         String?   @db.Uuid
  member_no       String?
  gender          String?
  prefix_th       String?
  fullname_th     String?
  email           String?
  phone_no        String?
  birthday        DateTime? @db.Date
  pdpa_accepted   Boolean?
  pdpa_version    String?
  tier            String?   @db.VarChar(20)
  tier_start_at   DateTime? @db.Timestamptz(6)
  tier_expired_at DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

view vw_discount_products_list {
  promotion_type  String?
  promotion_name  String?
  company_name    String?
  brand_name      String?
  category_name   String?
  mat_identity    String?
  product_name    String?
  unit            String?
  online_price    Float?
  sale_price      Float?
  sale_percent    Float?
  promotion_start DateTime? @db.Timestamptz(6)
  promotion_end   DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

view vw_flashsale_products_list {
  promotion_type      String?
  promotion_name      String?
  company_name        String?
  brand_name          String?
  category_name       String?
  mat_identity        String?
  product_name        String?
  unit                String?
  online_price        Float?
  sale_price          Float?
  sale_percent        Float?
  promotion_start     DateTime? @db.Timestamptz(6)
  promotion_end       DateTime? @db.Timestamptz(6)
  product_option_id   Int?
  url_image           String?
  promotion_id        Int?
  is_promotion_active Boolean?

  @@schema("3nconnect")
}

view vw_invoice_header_planetone {
  id              Int?
  created_at      DateTime? @db.Timestamptz(6)
  company_name    String?
  Entity_ID       String?   @map("Entity ID")
  Entity_Name     String?   @map("Entity Name")
  Address1        String?
  Address2        String?
  Province        String?
  Country         String?
  PostCode        String?
  Tax_Number      String?   @map("Tax Number")
  Branch_Tax_Name String?   @map("Branch Tax Name")
  Branch_Tax_ID   String?   @map("Branch Tax ID")

  @@schema("3nconnect")
}

view vw_planetone_grouping_stock {
  MATUnit  String?
  grandqty Int?
  company  String?

  @@schema("3nconnect")
}

view vw_planetone_stocks {
  company_name           String?
  MATUnit                String?
  Lot                    String?
  location_id            String?
  qty_balance            Int?
  qty_ready              Int?
  qty_pn_out             Int?
  qty_total              Int?
  unit                   String?
  expired                String?
  available_life         Int?
  unitcost               Float?
  costbalance            Float?
  po_no                  String?
  po_date                String?
  po_referrence          String?
  purchaser_personnel    String?
  pr_no                  String?
  mat_in                 String?
  mat_in_referrence      String?
  invoice_id_mat_in      String?
  received_datetime      String?
  qty_in_p_ready         Int?
  qty_in_p_ready_m_pnout Int?
  age                    Int?
  product_life           String?

  @@schema("3nconnect")
}

view vw_product_payment_methods {
  product_id        Int?
  product_option_id Int?
  product_name      String?
  option_name       String?
  credit_terms      Boolean?
  mobile_banking    Boolean?
  qr_code_promptpay Boolean?
  visa_card         Boolean?

  @@schema("3nconnect")
}

view vw_products {
  company_name      String?
  brand_name        String?
  category_name     String?
  mat_identity      String?
  product_name      String?
  option_name       String?
  unit              String?
  online_price      Float?
  product_option_id Int?
  url_image         String?
  is_online_active  Boolean?
  is_pre_order      Boolean?
  is_active         Boolean?

  @@schema("3nconnect")
}

view vw_promotion_bundle_deal_index {
  promotion_id                  Int?
  promotion_name                String?
  customer_tiers                String[]
  is_accept_overlapse_promotion Boolean?
  url_product_image             String?
  get_product_option_id         Int?
  get_product_name              String?
  get_product_price             Float?
  get_quantity                  Int?
  get_product_unit              String?
  free_url_image                String?
  free_product_option_id        Int?
  free_product_name             String?
  free_product_price            Float?
  free_quantity                 Int?
  free_product_unit             String?

  @@schema("3nconnect")
}

view vw_promotion_extra_points_bill_index {
  url_promotion_image           String?
  promotion_id                  Int?
  promotion_name                String?
  promotion_description         String?
  promotion_start               DateTime? @db.Timestamptz(6)
  promotion_end                 DateTime? @db.Timestamptz(6)
  is_accept_overlapse_promotion Boolean?
  level_no                      Int?
  min_amount                    Float?
  points_multiplier             Float?

  @@schema("3nconnect")
}

view vw_promotion_extra_points_products_index {
  url_promotion_image           String?
  promotion_id                  Int?
  promotion_name                String?
  promotion_description         String?
  promotion_start               DateTime? @db.Timestamptz(6)
  promotion_end                 DateTime? @db.Timestamptz(6)
  is_accept_overlapse_promotion Boolean?
  product_id                    Int?
  product_name                  String?
  product_option_id             Int?
  option_name                   String?
  points_multiplier             Float?
  url_image                     String?
  mat_identity                  String?

  @@schema("3nconnect")
}

view vw_promotion_grandtotal_bill_index {
  url_promotion_image              String?
  promotion_id                     Int?
  promotion_name                   String?
  promotion_description            String?
  promotion_start                  DateTime? @db.Timestamptz(6)
  promotion_end                    DateTime? @db.Timestamptz(6)
  bundle_deal_grand_total_tiers_id Int?
  level_no                         Int?
  minimum_grand_total              Float?
  is_accept_overlapse_promotion    Boolean?
  url_image                        String?
  product_id                       Int?
  product_name                     String?
  product_option_id                Int?
  option_name                      String?
  online_price                     Float?
  free_quantity                    Int?
  customer_tiers                   String[]

  @@schema("3nconnect")
}

view vw_promotion_products_index {
  product_id                    Int?
  product_option_id             Int?
  url_image                     String?
  category_hierarchy            String[]
  product_name                  String?
  unit                          String?
  mat_identity                  String?
  option_name                   String?
  promotion_type                String?
  online_price                  Float?
  promotion_id                  Int?
  is_accept_overlapse_promotion Boolean?
  sale_price                    Float?
  sale_percent                  Float?
  is_online_active              Boolean?
  is_pre_order                  Boolean?
  company_name                  String?
  company_id                    Int?

  @@schema("3nconnect")
}

view vw_reward_point_transactions_customeruser {
  user_id      String?   @db.Uuid
  created_at   DateTime? @db.Timestamptz(6)
  expired_at   DateTime? @db.Timestamptz(6)
  points       Int?
  redeem_point Decimal?  @db.Decimal
  redeem_exp   DateTime? @db.Date
  reason       String?

  @@schema("3nconnect")
}

view vw_sync_stock_ecommerce {
  id                     Int?
  created_at             DateTime? @db.Timestamptz(6)
  mat_unit_identity      String?
  ecommerce_mat_identity String?
  mat_name               String?
  mat_category_text      String?
  mat_status             String?
  mat_qty_unit           String?
  mat_agqty_unit         String?
  company_id             Int?
  company_name           String?
  min_price              Float?
  online_price           Float?
  sale_option_name       String?
  is_stock               Boolean?
  attributes_hierarchy   Int[]
  is_online_status       Boolean?

  @@schema("3nconnect")
}

view vw_tier_points_customeruser {
  total_points Int?
  user_id      String?   @db.Uuid
  expired_at   DateTime? @db.Timestamptz(6)

  @@schema("3nconnect")
}

view vw_total_points_customeruser {
  total_points Decimal? @db.Decimal
  user_id      String?  @db.Uuid

  @@schema("3nconnect")
}

view public_vw_bundle_deal_get_x_free_y {
  promotion_type        String?
  promotion_name        String?
  get_company_name      String?
  get_brand_name        String?
  get_product_category  String?
  get_mat_identity      String?
  get_product_name      String?
  get_product_unit      String?
  get_quantity          Int?
  free_company_name     String?
  free_brand_name       String?
  free_product_category String?
  free_mat_identity     String?
  free_product_name     String?
  free_product_unit     String?
  free_quantity         Int?
  promotion_start       DateTime? @db.Timestamptz(6)
  promotion_end         DateTime? @db.Timestamptz(6)

  @@map("vw_bundle_deal_get_x_free_y")
  @@schema("public")
}

view customer_peak_hour {
  customer_id String? @db.Uuid
  peak_hour   Int?

  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}
